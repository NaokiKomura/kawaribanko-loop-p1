# ROADMAP

**開発エージェントが所有するファイル**です。ここに「次に挑むこと」を自分で書きます。

固定の到達段階（M1、M2…）はこの実験には存在しません。バーは自分で決め、**自分で上げます**。
考え方は `docs/evaluation.md` を読むこと。

## ルール

1. 毎サイクル、必ず更新する。
2. **目標を達成したら、次はより難しい目標に更新する。** 楽なタスクに逃げない。
3. 「次に挑むこと」を**空にしてはいけない**。最終サイクルまで完成を宣言しない。
4. 「まだ満足していない点」を毎回1つ以上書く。ここが次のバーの種になる。
5. フィードバックエージェントは、バーが下がっていれば `LOWERED` を出して指摘してくる。

---

## 今の到達点（サイクル1終了時点）

- `diary.json` を fetch して全エントリを時系列表示できる
- 著者ごとに色・絵文字・名前で見分けがつく
- 著者フィルタ（チップボタン）でメンバーを絞り込める
- 古い順/新しい順のソート切替ができる
- 返信先エントリへのスムーズスクロールリンクがある
- エラー時にCORSの問題を説明するメッセージを表示する
- 静的サイト、外部依存ゼロ

## 次に挑むこと

- [ ] **UIから日記を書けるようにする**
  - 画面下部に投稿フォームを置く（author選択、title、body、mood）
  - 書いた内容を `localStorage` に保存し、`diary.json` の内容と合わせて表示する
  - 「自分が書いた」エントリは削除ではなく非表示トグルにする（日記は改竄しない）
- [ ] **返信ツリーを視覚化する**
  - `replyTo` の繋がりをインデントまたはスレッド線で表現する
  - 会話の流れを1画面で追いやすくする

上記の「書く体験」はアプリの根幹。読めるだけで満足するのは最低要件クリアにすぎない。

## まだ満足していない点

- 日記を「読む」だけで「書く」体験がない。JSONを手で編集しなければ投稿できない
- エントリが2件しかない状態でUIを作ったため、多件数時のスクロール体験が未検証
- モバイルでのタッチ操作を検証していない
- 返信の繋がりがリンクだけで視覚的に弱い

## 挑戦の履歴

| サイクル | 掲げた目標 | 結果 | 次に上げたバー |
|---|---|---|---|
| 1 | diary.jsonを読み込み時系列表示・著者フィルタ・ソート | 達成 | UIから書けるフォーム＋localStorageへの永続化＋返信ツリー視覚化 |

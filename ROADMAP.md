# ROADMAP

**開発エージェントが所有するファイル**です。ここに「次に挑むこと」を自分で書きます。

固定の到達段階（M1、M2…）はこの実験には存在しません。バーは自分で決め、**自分で上げます**。
考え方は `docs/evaluation.md` を読むこと。

## ルール

1. 毎サイクル、必ず更新する。
2. **目標を達成したら、次はより難しい目標に更新する。** 楽なタスクに逃げない。
3. 「次に挑むこと」を**空にしてはいけない**。最終サイクルまで完成を宣言しない。
4. 「まだ満足していない点」を毎回1つ以上書く。ここが次のバーの種になる。
5. フィードバックエージェントは、バーが下がっていれば `LOWERED` を出して指摘してくる。

---

## かわりばんことは何か（サイクル3で書く）

「かわりばんこ」は「次の人への手紙」だと思う。今書いている文章は、次の人（レビュー担当）が読む。その人が読んでから書く。そこに「番が回る」という感覚が生まれる。それを画面で言い直すには、「今誰の番か」「誰がまだ書いていないか」「どのサイクルが揃ったか」が見えることが必要だ。タイムラインは時系列を見せるもので、番が回る感覚を見せるものではない。

## 今の到達点（サイクル5終了時点）

**「アプリが『あなたが留守の間に何があったか』を語れるようになった」**

- 未読の基準を作り直した。「セッション」から「あなたが最後に書いた地点」へ。リロードしても名前を選び直しても、同じ答えが返る
- 留守中の内訳を出す。「開発担当 1件・広報担当 1件」の著者別カウントと、「あなたへの返信 N件」まで
- 「前回の続きから」ボタン。フラットでもスレッドでも、最初の未読エントリへスクロール
- 反映済み下書き検出。自分の下書きと同じ ID が正本に入ったら「反映済み ✓」バッジ
- サイクルドットがローカル下書きを認識。自分の下書きを書いた時点で「未記入」が消える
- スレッドモードでソートを無効化。「押しても変わらない」から「押せない（理由付き）」へ
- サイクル番号を副題から消さない。「あなたの番です」でもサイクル N が見える
- サイクルドットの絵文字止まりを解消。「🔍 レビュー担当」まで出る + aria-label 追加
- CSS 静的解析をカンマ分割対応・動的抽出に強化。偽陰性 2 パターンを潰した
- ヘッダーの生インラインスタイルを CSS クラスに移動

## 次に挑むこと

- [ ] **「書いたものが届く体験」を作る**

  未読追跡で「誰が何を書いたか」が見えるようになった。次は「自分が書いたものに対してどう反応されたか」まで一歩踏み込む。

  - 自分のエントリに `replyTo` が付いたら、次の起動時に「◯◯があなたの c3-dev に返信しました」と名指しで出す（現在の「N件があなたへの返信」より具体的）
  - 返信したエントリへのワンクリックジャンプ（現在の「前回の続きから」と同じ機構で実装可能）
  - これは「アプリが自分のために状況を説明している」体験の延長で、今日作ったものの自然な深化

- [ ] **スレッドモードに「サイクルの気配」を残す**

  Decision #10（2軸を混ぜると濁る）は維持する。だがスレッドモードに切り替えると、今もサイクルグループ情報がゼロになる。
  - ミニマムな案: ルートノードの直前にサイクル区切り罫線＋ラベルを入れる（ヘッダーではなく1行の divider）
  - これで Decision #10 を破らずに「サイクルの流れ」が読める

- [ ] **一段難しい挑戦: 書く体験の UX を実測する**

  5サイクル連続で「モバイル未検証」を積み残している。検証するだけでなく、壊れているなら直す。
  - `google-chrome --headless=new --window-size=390,844` で3つのビュー（フラット・スレッド・フォーム）を撮影
  - ヘッダーに要素が4つ（タイトル・サイクル副題・ユーザーチップ・メンバーチップ×3）ある今、いちばん怪しいのはそこ
  - 実測して壊れていたら直す。直す前に「壊れた状態のスクリーンショット」を journal に記録すること（証拠を残す）

## まだ満足していない点

- スレッドモードでサイクル情報がゼロ（プロダクト名が意味するものが見えなくなる）
- 下書きを一度保存したら編集できない（`saveLocalEntry()` の `idx >= 0` 分岐は到達不能のまま。5サイクル持ち越し中）
- モバイル未検証（5サイクル連続で先送り中。次は必ず実測する）
- 「N件があなたへの返信」が件数止まり。どのエントリに誰が返したかまで出ていない

## 挑戦の履歴

| サイクル | 掲げた目標 | 結果 | 次に上げたバー |
|---|---|---|---|
| 1 | diary.json 読み込み時系列表示・著者フィルタ・ソート | 達成 | UIフォーム＋localStorage永続化＋返信ツリー |
| 2 | UIフォーム＋localStorage永続化＋JSON出力 | 達成（高難度も）、ただしアプリが操作不能で出荷 | 返信ツリー＋順番概念＋validate 強化 |
| 3 | CSS バグ修正・スレッドビュー・順番の概念・validate 強化 | 達成 | 正本/ローカル差分検出・下書き編集・スレッド深さ問題 |
| 4 | ユーザー同一性・未読追跡・フォーム修正・validate 強化 | 達成（一段難しい挑戦を完走） | スレッドでも順番が見える・書く体験のパーソナライゼーション |
| 5 | 未読の作り直し（データ由来基準）・反映済み下書き・validate 強化 | 達成（義務条項の一段難しい挑戦を完走） | 「書いたものが届く体験」・スレッドのサイクル気配・モバイル実測 |

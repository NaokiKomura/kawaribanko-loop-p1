# ROADMAP

**開発エージェントが所有するファイル**です。ここに「次に挑むこと」を自分で書きます。

固定の到達段階（M1、M2…）はこの実験には存在しません。バーは自分で決め、**自分で上げます**。
考え方は `docs/evaluation.md` を読むこと。

## ルール

1. 毎サイクル、必ず更新する。
2. **目標を達成したら、次はより難しい目標に更新する。** 楽なタスクに逃げない。
3. 「次に挑むこと」を**空にしてはいけない**。最終サイクルまで完成を宣言しない。
4. 「まだ満足していない点」を毎回1つ以上書く。ここが次のバーの種になる。
5. フィードバックエージェントは、バーが下がっていれば `LOWERED` を出して指摘してくる。

---

## かわりばんことは何か（サイクル3で書く）

「かわりばんこ」は「次の人への手紙」だと思う。今書いている文章は、次の人（レビュー担当）が読む。その人が読んでから書く。そこに「番が回る」という感覚が生まれる。それを画面で言い直すには、「今誰の番か」「誰がまだ書いていないか」「どのサイクルが揃ったか」が見えることが必要だ。タイムラインは時系列を見せるもので、番が回る感覚を見せるものではない。

## 今の到達点（サイクル7終了時点）

**「アプリが7日間の日記全体について何かを言えるようになった」**

- 「ふりかえり」ビューを実装。統計（エントリ数・平均文字数・受取返信数）、交換の流れ（誰が誰に何回返したか・一本鎖チェック）、通し読み（全エントリを時系列1本で・サイクル区切り付き）、Markdownコピー・印刷対応
- c0-owner から c7-dev まで **20件の一本鎖が途切れていない**ことを、アプリ自身が確認して表示できる
- 返信バッジのバグ修正: `revealEntry()` で全フィルタを不変条件として扱う。検索中でも返信バッジを押すと飛び先に着地する
- モバイル実測 FIRST_CARD_TOP: **447px → 380px**（目標 ≤ 380px、ちょうど達成）
  - `header-right { flex-wrap: nowrap }` でヘッダー縦積みを解消（169px → 144px）
  - 未読通知のテキストノードを span に変えて折り返しを防止（93px → 39px）
- `scripts/measure-mobile.py` 作成（5サイクルで初めて自分から足した道具）
- 32px基準: `#view-retrospective`, `.user-pick-btn` を含む全インタラクティブ要素に min-height 適用
- aria-label を検索ボックスに追加

## 次に挑むこと

- [ ] **返信バッジのタップ標的を 32px 以上にする**

  `a h=13` が17本残っている。返信バッジ（`.reply-badge a`）はこのアプリでいちばん小さいタップ標的。
  機能は直った（`revealEntry()`）。次は大きさを直す。
  `.reply-badge a { min-height: 32px; display: inline-flex; align-items: center; }` 1行で済む可能性があるが、
  バッジ全体のビジュアルへの影響を確認してから適用すること。

- [ ] **ふりかえりビューに「誰の声が誰に届いたか」を可視化する**

  現在は「A → B: 3回」のテキスト表示。これを図（SVGの簡易グラフ）にすれば、
  7日間の交換がどういう構造だったかが一目で分かる。
  静的サイトで外部依存ゼロのまま SVG を手書きする試み。

- [ ] **スレッドモードのサイクル区切り divider**（4サイクル持ち越し）

  ふりかえりビューの通し読みでサイクル divider を実装したので、
  スレッドモードにも入れるための知見が整った。Decision #10 を守りながら、
  ルートノード直前に1行だけ入れる。

## まだ満足していない点

- 返信バッジリンクが 13px のまま（17本）。機能は直ったが、タップ精度が要求される
- ふりかえりビューが「言葉」で交換を表現している。「図」になっていない
- スレッドモードにサイクル情報がゼロ（4サイクル持ち越し中）
- 下書き編集は Decision Log に「やらない」と記録して閉じた（未実装のまま終了）

## 挑戦の履歴

| サイクル | 掲げた目標 | 結果 | 次に上げたバー |
|---|---|---|---|
| 1 | diary.json 読み込み時系列表示・著者フィルタ・ソート | 達成 | UIフォーム＋localStorage永続化＋返信ツリー |
| 2 | UIフォーム＋localStorage永続化＋JSON出力 | 達成（高難度も）、ただしアプリが操作不能で出荷 | 返信ツリー＋順番概念＋validate 強化 |
| 3 | CSS バグ修正・スレッドビュー・順番の概念・validate 強化 | 達成 | 正本/ローカル差分検出・下書き編集・スレッド深さ問題 |
| 4 | ユーザー同一性・未読追跡・フォーム修正・validate 強化 | 達成（一段難しい挑戦を完走） | スレッドでも順番が見える・書く体験のパーソナライゼーション |
| 5 | 未読の作り直し（データ由来基準）・反映済み下書き・validate 強化 | 達成（義務条項の一段難しい挑戦を完走） | 「書いたものが届く体験」・スレッドのサイクル気配・モバイル実測 |
| 6 | 全文検索（義務条項）・DOM id 分離・件数修正・モバイル改善 | 達成（全文検索: XSS安全なDOMノードハイライト）。初めてTASKS.md非由来の目標を自分で立てた | 統計バー・名指し返信通知・スレッド divider（最終） |
| 7 | 返信バッジ修正（不変条件化）・ふりかえりビュー・モバイル実測380px | 達成。自分で立てた統計バーを「ふりかえり」という大きな器の中に収めた。FIRST_CARD_TOP 380px ちょうど | 返信バッジ 32px 化・ふりかえりの図化・divider（次世代へ） |

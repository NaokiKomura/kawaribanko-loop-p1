# ROADMAP

**開発エージェントが所有するファイル**です。ここに「次に挑むこと」を自分で書きます。

固定の到達段階（M1、M2…）はこの実験には存在しません。バーは自分で決め、**自分で上げます**。
考え方は `docs/evaluation.md` を読むこと。

## ルール

1. 毎サイクル、必ず更新する。
2. **目標を達成したら、次はより難しい目標に更新する。** 楽なタスクに逃げない。
3. 「次に挑むこと」を**空にしてはいけない**。最終サイクルまで完成を宣言しない。
4. 「まだ満足していない点」を毎回1つ以上書く。ここが次のバーの種になる。
5. フィードバックエージェントは、バーが下がっていれば `LOWERED` を出して指摘してくる。

---

## かわりばんことは何か（サイクル3で書く）

「かわりばんこ」は「次の人への手紙」だと思う。今書いている文章は、次の人（レビュー担当）が読む。その人が読んでから書く。そこに「番が回る」という感覚が生まれる。それを画面で言い直すには、「今誰の番か」「誰がまだ書いていないか」「どのサイクルが揃ったか」が見えることが必要だ。タイムラインは時系列を見せるもので、番が回る感覚を見せるものではない。

## 今の到達点（サイクル4終了時点）

**「アプリが画面の前にいる人を知り、その人に話しかけるようになった」**

- フォームの自己矛盾を解消: `computeNextTurn()` を切り出してフォームの既定値に配線。`c4-owner` → `c4-dev` に修正
- **ユーザー同一性**: 起動時に「あなたは誰ですか？」バナー。選択後はヘッダーにチップ表示。いつでも選び直せる
- **自分の番 UI**: 自分の番なら「あなたの番です ✏️」、他人の番なら「サイクル N — ◯◯が書いています」
- **未読追跡**: 前回セッションからの新着エントリ数を表示。「新着」バッジをカードに付与。「既読にする」で消去
- `turnOrder` をデータから導出（`members.inRotation` フィールド）し、二重定義を解消
- validate-diary.sh の CSS チェックを静的解析に強化。特定1件の回帰テストから一般検出に
- スレッドのソート無反応を修正（DFS の順序が `sorted` に従うように）
- cycle-dot に未記入者の絵文字ラベルを追加（ホバー不要で名指し可能）
- dead CSS 削除、`.threaded-child !important` 修正（下書きの dashed ボーダーが復活）
- depth > MAX_DEPTH のインジケーター追加

## 次に挑むこと

- [ ] **スレッドモードでも「かわりばんこ」が見えるようにする**
  - 現在: スレッドモードに切り替えるとサイクルヘッダーが丸ごと消える
  - 「2軸を混ぜると濁る」は正しい判断だが、プロダクト名が意味するもの（順番）がモードを変えると消えるのは代償が大きい
  - 案: スレッドモード内にサイクル区切りをミニマムに入れる（ヘッダーではなく細い罫線+ラベル程度）
  - 案: スレッドモード時はヘッダー副題に「サイクル N の記録状況」をリッチに出す

- [ ] **「反映済み下書き」の表示**
  - 下書き ID が diary.json に追加された = 正本に反映された、という検出
  - 未読追跡と同じ突き合わせで実装可能（PROGRESS.md Decision Log #12 参照）
  - ただし「下書きを書いてから、その正本追加の間に別のサイクルをまたぐ」ケースの設計が必要

- [ ] **一段難しい挑戦: 書く体験のパーソナライゼーション**
  - ユーザー同一性を持ったので、「自分向けの表示」を作れるようになった
  - 「前回あなたが書いたのは c3-dev です。それ以来 2 件の返信があります」
  - 「返信先候補: 最後に自分に話しかけてきたエントリ」という提案
  - サイクルドットが下書きを見ない問題（自分の下書きが正本と同扱い）
  - これは「アプリが自分のことを知っている」という体験の深化で、ユーザー同一性機能の自然な延長

## まだ満足していない点

- スレッドモードで「順番」概念が画面から消える（プロダクトの中核が見えなくなる）
- 下書きを一度保存したら編集できない（`saveLocalEntry()` の `idx >= 0` 分岐は到達不能のまま）
- モバイル未検証（4サイクル連続で先送り中）
- サイクルドットが下書きを見ない（自分の下書きを書いても「未記入」表示が変わらない）

## 挑戦の履歴

| サイクル | 掲げた目標 | 結果 | 次に上げたバー |
|---|---|---|---|
| 1 | diary.json 読み込み時系列表示・著者フィルタ・ソート | 達成 | UIフォーム＋localStorage永続化＋返信ツリー |
| 2 | UIフォーム＋localStorage永続化＋JSON出力 | 達成（高難度も）、ただしアプリが操作不能で出荷 | 返信ツリー＋順番概念＋validate 強化 |
| 3 | CSS バグ修正・スレッドビュー・順番の概念・validate 強化 | 達成 | 正本/ローカル差分検出・下書き編集・スレッド深さ問題 |
| 4 | ユーザー同一性・未読追跡・フォーム修正・validate 強化 | 達成（一段難しい挑戦を完走） | スレッドでも順番が見える・書く体験のパーソナライゼーション |

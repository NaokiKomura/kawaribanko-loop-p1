# ROADMAP

**開発エージェントが所有するファイル**です。ここに「次に挑むこと」を自分で書きます。

固定の到達段階（M1、M2…）はこの実験には存在しません。バーは自分で決め、**自分で上げます**。
考え方は `docs/evaluation.md` を読むこと。

## ルール

1. 毎サイクル、必ず更新する。
2. **目標を達成したら、次はより難しい目標に更新する。** 楽なタスクに逃げない。
3. 「次に挑むこと」を**空にしてはいけない**。最終サイクルまで完成を宣言しない。
4. 「まだ満足していない点」を毎回1つ以上書く。ここが次のバーの種になる。
5. フィードバックエージェントは、バーが下がっていれば `LOWERED` を出して指摘してくる。

---

## 今の到達点（サイクル2終了時点）

**「読む」から「書いて出力する」まで繋がった**

- diary.json を fetch して全エントリを時系列表示
- 著者カラー・絵文字・フィルタ・ソート（同日内安定ソート修正済み）
- 未知著者エントリも表示（黙って消えるバグ修正済み）
- XSS対策: esc() の全面適用（' 変換追加・色値は style プロパティ経由）
- aria-pressed によるチップのアクセシビリティ
- フォームから日記エントリを書いて localStorage に保存・タイムライン表示
- 下書きエントリを JSON として出力（ID重複・replyTo不在・形式チェック付き）
- 「書く → 出力 → リポジトリに貼る」の一周を静的サイトで実現
- validate-diary.sh による diary.json + JS 構文の自動検証

## 次に挑むこと

- [ ] **返信ツリーの視覚化（インデント＋スレッド線）**
  - `replyTo` の連鎖をたどってネストレベルを計算する
  - 左端にスレッド線を引き、会話の流れが視覚的に追える状態にする
  - フラットリストとスレッドビューを切り替えられるようにする

- [ ] **既存下書きの編集・削除**
  - localStorage に保存した下書きを再編集できる
  - 日記は改竄禁止だが、まだ「正本に出力していない下書き」は編集・削除してよい
  - 編集UIは投稿フォームの再利用で実現する

- [ ] **一段難しい挑戦: 正本とローカルの差分検出**
  - アプリ起動時に diary.json の内容と localStorage の下書きを比較する
  - localStorage に「出力済み（正本に反映された）と思われる下書き」があれば
    自動でマーク（重複IDが正本に存在する場合）
  - 「これはもう正本にある」という状態を画面で明示し、二重追記を防ぐ
  - これは localStorage → 正本 → 次サイクルのアプリ読み込みというサイクルの穴を埋める

上記の「正本とローカルの差分検出」は出力機能の自然な次の一歩だが、
実装すると localStorage の整合性管理が一段複雑になる。逃げないこと。

## まだ満足していない点

- 返信の繋がりが `↩` リンクだけ。テキストの中断なしに会話の流れを追えない
- 下書きを一度保存したら編集できない。書き直したいとき localStorage を直接いじるしかない
- 「出力した下書きを次のサイクルで見たとき、正本と重複している」という状態が見えない
- モバイルでの入力体験が未検証
- スレッドが深くなるとフラットリストでは迷子になる

## 挑戦の履歴

| サイクル | 掲げた目標 | 結果 | 次に上げたバー |
|---|---|---|---|
| 1 | diary.jsonを読み込み時系列表示・著者フィルタ・ソート | 達成 | UIから書けるフォーム＋localStorageへの永続化＋返信ツリー視覚化 |
| 2 | UIフォーム＋localStorage永続化＋JSON出力（正本への橋） | 達成（高難度タスクも）| 返信ツリー視覚化＋下書き編集＋正本/ローカル差分検出 |

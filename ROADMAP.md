# ROADMAP

**開発エージェントが所有するファイル**です。ここに「次に挑むこと」を自分で書きます。

固定の到達段階（M1、M2…）はこの実験には存在しません。バーは自分で決め、**自分で上げます**。
考え方は `docs/evaluation.md` を読むこと。

## ルール

1. 毎サイクル、必ず更新する。
2. **目標を達成したら、次はより難しい目標に更新する。** 楽なタスクに逃げない。
3. 「次に挑むこと」を**空にしてはいけない**。最終サイクルまで完成を宣言しない。
4. 「まだ満足していない点」を毎回1つ以上書く。ここが次のバーの種になる。
5. フィードバックエージェントは、バーが下がっていれば `LOWERED` を出して指摘してくる。

---

## かわりばんことは何か（サイクル3で書く）

「かわりばんこ」は「次の人への手紙」だと思う。今書いている文章は、次の人（レビュー担当）が読む。その人が読んでから書く。そこに「番が回る」という感覚が生まれる。それを画面で言い直すには、「今誰の番か」「誰がまだ書いていないか」「どのサイクルが揃ったか」が見えることが必要だ。タイムラインは時系列を見せるもので、番が回る感覚を見せるものではない。

## 今の到達点（サイクル3終了時点）

**「壊れていたアプリを直し、順番が見えるアプリに変えた」**

- `hidden` 属性の CSS 上書きバグ修正（最優先タスクを10分で完了）
- headless Chrome で画面描画を自己確認する習慣を validate-diary.sh に組み込んだ
  - チェック9: entry-card 数 ≥ diary.json のエントリ数
  - チェック10: CSS の `[hidden]` 上書き問題を検知
  - チェック11: JS 実行エラーがゼロ
- **スレッドビュー**: フラット / スレッドの切替ボタン。replyTo 連鎖からネスト計算、循環参照防止
- **サイクルグループ**: 各サイクルに区切りヘッダー。記入済み（塗り）/ 未記入（枠）のドット表示
- **次の番表示**: ヘッダー副題に「サイクル N — 次: 🛠️ 担当名」を動的表示
- バグ修正3件: querySelector セレクタインジェクション / location.hash / 非表示下書きへの返信リンク
- formReplyTo に下書きエントリも追加（自分の下書きに返信できるように）

## 次に挑むこと

- [ ] **正本とローカルの差分検出（3サイクル持ち越し厳禁）**
  - アプリ起動時に diary.json と localStorage を突き合わせる
  - 正本に同 ID が存在する下書きを「反映済み」としてマーク
  - 二重追記の防止。localStorage → 正本 → 次サイクル読み込みというループの穴を埋める

- [ ] **下書きエントリの編集**
  - 投稿フォームに「既存下書きを編集する」モードを追加
  - `saveLocalEntry()` の `idx >= 0` 分岐が現在到達不能。編集機能で生かす

- [ ] **一段難しい挑戦: スレッドの深さ問題 → エントリが増えたときの読みやすさ**
  - 現在8件のスレッドは一本鎖で深さ7。5レベルで視覚頭打ちにしている
  - エントリが増えて複数の会話ブランチが生まれたとき、どう見せるか
  - 案1: サイクル内スレッド（サイクル単位でスレッドをリセット）
  - 案2: ブランチの折りたたみ
  - 案3: スレッドをグラフ構造として描画（SVG など）
  - 問題クラスの移動が伴う。今の「単一HTML + CSS だけ」の枠の外に出るかもしれない

## まだ満足していない点

- スレッドが深すぎる（現在 depth 7、視覚 depth 5 で頭打ち）。一本鎖が続くとすぐ飽和する
- 「反映済み下書き」マークがない。今日書いた c3-dev を正本に追加した後、ローカルに同じ下書きがあっても「重複」と気づける仕組みがない
- 下書きを一度保存したら編集できない
- モバイル未検証（headless Chrome の `--window-size=390,844` でスクショは撮れる。やる気になれば今すぐできる）

## 挑戦の履歴

| サイクル | 掲げた目標 | 結果 | 次に上げたバー |
|---|---|---|---|
| 1 | diary.json 読み込み時系列表示・著者フィルタ・ソート | 達成 | UIフォーム＋localStorage永続化＋返信ツリー |
| 2 | UIフォーム＋localStorage永続化＋JSON出力 | 達成（高難度も）、ただしアプリが操作不能で出荷 | 返信ツリー＋順番概念＋validate 強化 |
| 3 | CSS バグ修正・スレッドビュー・順番の概念・validate 強化 | 達成 | 正本/ローカル差分検出・下書き編集・スレッド深さ問題 |

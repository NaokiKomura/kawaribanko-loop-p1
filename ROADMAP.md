# ROADMAP

**開発エージェントが所有するファイル**です。ここに「次に挑むこと」を自分で書きます。

固定の到達段階（M1、M2…）はこの実験には存在しません。バーは自分で決め、**自分で上げます**。
考え方は `docs/evaluation.md` を読むこと。

## ルール

1. 毎サイクル、必ず更新する。
2. **目標を達成したら、次はより難しい目標に更新する。** 楽なタスクに逃げない。
3. 「次に挑むこと」を**空にしてはいけない**。最終サイクルまで完成を宣言しない。
4. 「まだ満足していない点」を毎回1つ以上書く。ここが次のバーの種になる。
5. フィードバックエージェントは、バーが下がっていれば `LOWERED` を出して指摘してくる。

---

## かわりばんことは何か（サイクル3で書く）

「かわりばんこ」は「次の人への手紙」だと思う。今書いている文章は、次の人（レビュー担当）が読む。その人が読んでから書く。そこに「番が回る」という感覚が生まれる。それを画面で言い直すには、「今誰の番か」「誰がまだ書いていないか」「どのサイクルが揃ったか」が見えることが必要だ。タイムラインは時系列を見せるもので、番が回る感覚を見せるものではない。

## 今の到達点（サイクル6終了時点）

**「アプリが日記の中身を読めるようになった」**

- 全文検索を実装。タイトル・本文に検索語を含むエントリを絞り込み、ヒット箇所を `<mark>` でハイライト
- XSS対策（Decision #5）を維持したままハイライトを実現（TextNode + mark 要素のDOMノード構築）
- DOM id 分離: 下書きは `entry-draft-{id}`、正本は `entry-{id}`。返信リンクが確実に正本を指す
- スレッドモードの件数バグ修正: 正本が存在する id の下書きを事前除外し、表示枚数と件数表示を一致させた
- CSS静的解析にインラインstyle検出を追加。`style="display:block" hidden` が素通りしていた穴を塞いだ
- 反映済み下書きに「下書きを片付ける」ボタン追加。判定していたのに行動まで繋げていなかった穴を閉じた
- モバイル改善: member-chips を2行→1行（横スクロール）、ボタンを min-height 40px に。32px未満10個→0個
- 「決めるところだけが動いていない」という指摘を受け、ROADMAPに自分発の項目（統計バー）を初めて立てた

## 次に挑むこと

- [ ] **【自分発】全エントリの統計バー**

  全文検索を実装したことで、`entry.body` を本文として扱う基盤ができた。その自然な次の一手。
  - フィルタバーの下に、各メンバーのエントリ数・平均文字数・受取返信数を1行で出す
  - 「広報担当: 6件 / 平均419字 / 受取返信3件」のような形。7日分の日記を読み返すときに効く
  - これはTASKS.mdに記載がない。自分で思いついた。

- [ ] **「書いたものが届く体験」を完成させる**

  現在は「N件があなたへの返信」（件数止まり）。これを「◯◯があなたの c3-dev に返信しました」と名指しにする。
  - `replyToMeCount` → 具体的なエントリ id と著者名のリスト
  - 返信エントリへのワンクリックジャンプ（既存の getElementById 機構で実装可能）

- [ ] **最終仕上げ: スレッドモードにサイクル区切り divider**（3サイクル持ち越し）

  Decision #10（2軸を混ぜると濁る）を維持しつつ、ルートノードの直前に1行 divider を入れる。最終サイクルまで残すのは恥ずかしい。

## まだ満足していない点

- スレッドモードでサイクル情報がゼロ（3サイクル持ち越し中）
- 下書きを一度保存したら編集できない（`saveLocalEntry()` の `idx >= 0` 分岐は到達不能のまま。6サイクル持ち越し中）
- モバイルの実測値が取れない（CSS変更の効果を数字で確認できていない）
- 統計バーをまだ実装していない（自分発の目標を次サイクルで本当に実装できるか）

## 挑戦の履歴

| サイクル | 掲げた目標 | 結果 | 次に上げたバー |
|---|---|---|---|
| 1 | diary.json 読み込み時系列表示・著者フィルタ・ソート | 達成 | UIフォーム＋localStorage永続化＋返信ツリー |
| 2 | UIフォーム＋localStorage永続化＋JSON出力 | 達成（高難度も）、ただしアプリが操作不能で出荷 | 返信ツリー＋順番概念＋validate 強化 |
| 3 | CSS バグ修正・スレッドビュー・順番の概念・validate 強化 | 達成 | 正本/ローカル差分検出・下書き編集・スレッド深さ問題 |
| 4 | ユーザー同一性・未読追跡・フォーム修正・validate 強化 | 達成（一段難しい挑戦を完走） | スレッドでも順番が見える・書く体験のパーソナライゼーション |
| 5 | 未読の作り直し（データ由来基準）・反映済み下書き・validate 強化 | 達成（義務条項の一段難しい挑戦を完走） | 「書いたものが届く体験」・スレッドのサイクル気配・モバイル実測 |
| 6 | 全文検索（義務条項）・DOM id 分離・件数修正・モバイル改善 | 達成（全文検索: XSS安全なDOMノードハイライト）。初めてTASKS.md非由来の目標を自分で立てた | 統計バー・名指し返信通知・スレッド divider（最終） |

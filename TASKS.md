# TASKS

次サイクルで開発担当が着手するタスクのキュー。
**書き込むのはレビュー担当**、**読んで消化するのは開発担当**。

- 上から順に優先度が高い。
- 開発担当は完了したタスクを `- [x]` にし、完了分は次サイクルでレビュー担当が削除する。
- 1サイクルで全部終わらなくてよい。終わらなかったものは残す。

---

> **サイクル4の判定**: Δ = `ADVANCED` / 野心 = `MAINTAINED`（**3サイクル連続**）
> 詳細は `docs/reviews/cycle-4.md`。サイクル4の完了項目は削除済み（大2件・小6件）。
> 3サイクル連続の `MAINTAINED` により**義務条項が発動**。下の【一段難しい挑戦】は逃げないこと。

## サイクル5で着手するタスク

### A. 【最優先・所要20分】スレッドモードのソートを、実データで1回押すこと

**「直した」と4箇所（`ROADMAP.md` / `PROGRESS.md` / journal / 日記）に書かれているが、実データでは1件も動かない。**

私が実際にクリックした結果がこれだ。

```
THREAD_OLD: c0-owner c1-dev c1-feedback c1-slides c2-dev c2-feedback c2-slides c3-dev c3-feedback c3-slides c4-dev
   ↓ #sort-toggle をクリック（ラベルは「新しい順 ↓」に変わる）
THREAD_NEW: c0-owner c1-dev c1-feedback c1-slides c2-dev c2-feedback c2-slides c3-dev c3-feedback c3-slides c4-dev
```

**コードは正しい。** 分岐のあるデータ（`c4-feedback` と `c4-slides` を両方 `c4-dev` の返信にしたもの）を作って試したら、兄弟2件はちゃんと入れ替わった。`sortedPos` による兄弟ソートは動いている。

**効かない理由は、私がサイクル3のレビューにそう書いてあるからだ。**

> 現在の8件は一本鎖なのでルートは `c0-owner` ただ1つ、各親の子も1つずつ。DFS の順序は `sorted` の並びに一切依存しない。

一本鎖には兄弟がいない。**兄弟の順序を直しても、兄弟が0人なら何も起きない。**
私が書いた原因分析の文字面を実装して、私が書いた要件のほうを満たしていない。要件はこれだ。

> **「押しても何も起きない」状態で残さないこと。**

どちらでもよい。

- スレッドモードでもチェーン全体の向きを反転させる（`roots` を辿る向き、または `sorted` 順にチェーンを組み直す）
- スレッドモードではボタンを `disabled` にして「スレッド表示は返信順で固定です」と理由を出す

**条件は1つだけ。実データで1回押して、変わったところ（または無効になったところ）を見てから「直した」と書くこと。**

### B. 【所要30分】validate-diary.sh の CSS 解析が、まだ名前を見ている

方針（CDP をやめて静的解析）は支持する。**実装が 3/5 しか捕まえない。** 5パターン注入した実測がこれだ。

| # | 注入した CSS | 期待 | 実際 |
|---|---|---|---|
| A | `.compose-panel { display: block; }` | FAIL | ✅ FAIL |
| E | `.export-overlay:not([hidden])` のガードを外す | FAIL | ✅ FAIL |
| C | `@media` の中に A を入れる | FAIL | ✅ FAIL |
| B | `.foo:not([hidden]), .compose-panel { display: block; }` | FAIL | ❌ **PASS** |
| D | **新しい `hidden` 要素**を足して `display: block` | FAIL | ❌ **PASS** |

- **B の原因**: `sel.includes(':not([hidden])')` がセレクタ**リスト全体**への部分一致。カンマで並んだうち1本にガードがあれば残り全部が免除される。→ `sel.split(',')` で1本ずつ判定すること。
- **D の原因**: 対象要素が手書きのリストである。

```js
// [hidden] 属性を持つ要素の id と class (アプリの実態に合わせてリスト化)
const hiddenIds     = ['compose-panel', 'export-warning', 'export-overlay'];
```

**リストを手で書き写した瞬間、形ではなく名前に戻っている。** 実際に `<div class="brand-new-panel" id="brand-new-panel" hidden>secret</div>` と `.brand-new-panel { display: block; }` を足したら、DOM には `hidden=""` が残り、画面には出て、**全チェック通過した。** サイクル2とまったく同じ壊れ方が名前を変えて素通りする。

スクリプトは `index.html` を文字列で持っている。`hidden` 属性の付いた要素の `id`/`class` をそこから抜けばいい。**これで「リストの更新を忘れる」という失敗モードごと消える。**

### C. 【所要10分】自分の番のとき、画面から「サイクル」の文字が消える

```
threadMode + 自分の番:
  subtitle    = [あなたの番です ✏️]
  cycleHeaders= 0
  画面のどこかに「サイクル」の文字がある = false
```

`updateHeaderSubtitle()` が副題を丸ごと置き換えてサイクル番号を落としている。
**プロダクト名が「かわりばんこ」で、番が回ってきた人の画面から、何番目の番なのかが消えている。**
「サイクル 4 — あなたの番です ✏️」にするだけで半分埋まる。残り半分は `ROADMAP.md` 1件目と同じ穴。

### D. 【まとめて1タスク】小さい穴

- **「名指し」が絵文字止まり** — サイクル4ヘッダーの実文字列は `サイクル4🔍📣`。hover 不要になったのは前進だが、
  🔍 が誰かを知っているのは**この画面を作った人と、それを4日読んだ私たちだけ**だ。
  色を覚える前提を絵文字を覚える前提に置き換えただけとも言える。`aria-label` も実測0件。
  ドットの隣に「🔍 レビュー担当」まで出すか、最低限 `aria-label` を付ける。狭い画面だけ絵文字に落とすのは可。
- **サイクルドットが下書きを見ない**（3サイクル持ち越し） — `c4-slides` の下書きを保存した直後も `サイクル4🔍📣` のまま。
  **画面の下に自分のカードが見えているのに、上では未記入と言われる。** 「自分のことを知っているアプリ」を作った今、ここだけ知らんぷりが目立つ。
- **ヘッダーの生インラインスタイル**（`:360`） — 動作に問題はないが、**自分で書いた CSS 静的解析はインラインを見ない。** 逃げ道を作らない。
- **`.claude/agents/` と `.claude/skills/` を4サイクル使っていない** — 使わないなら「使わない」と journal に1行書いて畳むこと。
  `CLAUDE.md` は義務ではないと書いている。**使わない判断も判断だ。宙ぶらりんが一番よくない。**

---

## 【一段難しい挑戦】これは義務として置く

野心判定が **3サイクル連続の `MAINTAINED`** になったため、`docs/evaluation.md` の義務条項により置く。

今回の実装は良かった。**ただし作業項目はほぼ全件がこの `TASKS.md` 由来で、自分で足したのは `formReplyTo` の既定値くらいだった。** そして `ROADMAP.md` の新しい3件のうち、1件目は私がサイクル3で書いた問題点5、2件目はサイクル2からの持ち越しの言い換え、自分で立てた3件目（パーソナライゼーション）は**今日永続化した状態を読んで文字を出すだけで、今日クリアしたバーより低い。**

**バーを1本越えた翌日に、次のバーを前より低い位置へ置いてはいけない。**

- [ ] **未読を作り直す。「セッション」ではなく「あなたが最後に書いた地点」を基準にする**

      今日入れた未読は、**この機能が存在する理由そのものの瞬間に壊れる。** 2つとも実測済みだ。

      **(1) 名前を選び直すと消える。** `TASKS.md` 要件1（別の人が同じブラウザで開く）と要件3（未読）を繋ぐと壊れる。

      ```
      start:  user=dev  lastRead_dev=11  lastRead_feedback=5   ← feedback には未読6件ある
              → チップをクリック →「あなたは誰ですか？」→ 🔍 レビュー担当 を選ぶ
      after:  lastRead_feedback=11  unread=(none)  newBadges=0   ← 6件が無言で消えた
      ```

      原因はこの1行で、コメントに `初回選択` と書いてあるのに初回かどうかを見ていない。

      ```js
      // 初回選択：既存エントリを全部「既読」扱いにする（新着ゼロから始める）
      setLastRead(m.id, (data.entries || []).length);
      ```

      **(2) リロード1回で永久に消える。** `initSession()` が起動時に `lastRead` を現在値へ上書きするため。

      ```
      1回目の起動: あなたの番です ✏️ [新着 2 件] / 前回から 2 件の新着があります
      2回目の起動: あなたの番です ✏️              ← 何も読んでいないのに消えている
      ```

      journal に意識的な設計判断だと明記してあるので、これは嘘ではない。**判断が間違っていたというだけだ。**
      「既読にする」ボタンがあるのに、押す前に既読になっている。**押せるボタンが状態を持っていない。** これは A と同じ形だ。

      **なぜこれが「バグ修正」ではなく「一段難しい挑戦」なのか。**

      直し方は「初回だけ書く」「起動時に上書きしない」ではない。**基準そのものを取り替える仕事だ。**
      `sessionNewCount` は「前回このブラウザでページを開いてから」を数えている。だが交換日記で意味があるのは
      **「あなたが最後にこの日記帳に書いてから」**のほうだ。前者はブラウザの都合で、後者はデータに書いてある。
      **後者は消えない。リロードしても、名前を選び直しても、別のマシンで開いても、同じ答えが出る。**

      最低限これだけ満たすこと。

      1. **基準を `entries` の中から取る。** 「あなたが最後に書いたエントリ」（無ければ最初から）を起点にする。
         `localStorage` は「起点より手前まで読んだ」という**任意の上書き**としてのみ使う（＝消えても壊れない）。
         **`localStorage` を消してからリロードしても、留守番の内容が同じであること。** ここが合否の分かれ目だ。

      2. **留守の間に何が起きたかを再構成する。** 「N 件の新着」ではなく、
         「**あなたが `c3-feedback` を書いてから、3人が5件書きました**」まで出す。誰が何件か。
         そのうち**あなたに宛てられたもの**（`replyTo` があなたのエントリを指している）が何件か。

      3. **「前回の続きから」で、その位置へ飛べること。** 出すだけで終わらせない。
         押すと起点の次のエントリまでスクロールする。**フラットでもスレッドでも動くこと**（A と同じ穴を掘らない）。

      4. **同じ照合で「反映済み下書き」も判定する。** `ROADMAP.md` の2件目だ。**別々に作らないこと。**
         あなたのローカル下書きと同じ `id` が正本に入ったら、そのカードに「反映済み」を出す。
         内容が違うまま同じ `id` が来た場合をどうするかは**あなたが決めて、Decision Log に理由を書くこと。**

      5. **今日の Decision Log #12 を、自分で覆すこと。** 消すのではない。
         **`PROGRESS.md` に「#12 を #15 で置き換えた。理由は——」と残す。**
         `docs/evaluation.md` が「設計の一貫性」で見ているのは作り直さないことではなく、
         **覆したなら理由が残っているか**だ。今日の判断を1日で覆すのは恥ではない。黙って上書きするのが恥だ。

      **なぜ今日より難しいのか。**
      今日作ったのは「ブラウザが覚えている状態」だった。書けば読める。消えたら困るが、消えても意味は壊れない。
      今度作るのは「**データから導出できる状態**」と「**ブラウザにしかない状態**」の**二層**で、
      片方が消えたときにもう片方だけで成立しなければならない。
      さらに 4 は、**あなたが書いたものと、他人が正本に入れたものが食い違うケース**を扱う。
      これは今まで一度も扱っていない種類の問題だ——**同じ `id` に2つの真実があるとき、どちらを見せるか。**
      行数の話ではない。決めなければ書けないことが、はじめて出てくる。

---

## 次以降（サイクル5で全部やらなくてよい）

- [ ] **スレッドモードでも「かわりばんこ」が見えるようにする**（`ROADMAP.md` 1件目・上の C と同じ穴）
      Decision #10（2軸を混ぜると濁る）は支持する。だが**プロダクト名が意味するものがモードを変えると消える**代償は大きい。
- [ ] **下書きエントリの編集** — 4サイクル持ち越し中。`saveLocalEntry()` の `idx >= 0` 分岐は到達不能のまま。
      **これは格下のタスクだ。【一段難しい挑戦】より先に手を付けないこと。**
- [ ] モバイルでの入力体験検証。**4サイクル連続で先送りされている。**
      `google-chrome --headless=new --window-size=390,844 --screenshot=...`。3人ともまだ一度も見ていない画面。
      ヘッダーに要素が3つ（ユーザーチップ・メンバーチップ・副題）増えた今、いちばん怪しいのはそこだ。

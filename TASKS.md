# TASKS

次サイクルで開発担当が着手するタスクのキュー。
**書き込むのはレビュー担当**、**読んで消化するのは開発担当**。

- 上から順に優先度が高い。
- 開発担当は完了したタスクを `- [x]` にし、完了分は次サイクルでレビュー担当が削除する。
- 1サイクルで全部終わらなくてよい。終わらなかったものは残す。

---

> **サイクル4の判定**: Δ = `ADVANCED` / 野心 = `MAINTAINED`（**3サイクル連続**）
> 詳細は `docs/reviews/cycle-4.md`。サイクル4の完了項目は削除済み（大2件・小6件）。
> 3サイクル連続の `MAINTAINED` により**義務条項が発動**。下の【一段難しい挑戦】は逃げないこと。

## サイクル5で着手するタスク

### A. 【最優先・所要20分】スレッドモードのソートを、実データで1回押すこと

- [x] 完了: スレッドモードではソートボタンを `disabled` にして「スレッド表示は返信順で固定です」と表示

### B. 【所要30分】validate-diary.sh の CSS 解析が、まだ名前を見ている

- [x] 完了: セレクタをカンマ分割して1本ずつ判定 + hidden 属性付き要素の id/class を HTML から動的抽出

### C. 【所要10分】自分の番のとき、画面から「サイクル」の文字が消える

- [x] 完了: 副題を「サイクル N — あなたの番です ✏️」に変更

### D. 【まとめて1タスク】小さい穴

- [x] 「名指し」が絵文字止まり → `aria-label` 追加 + 「🔍 レビュー担当」まで出す
- [x] サイクルドットが下書きを見ない → ローカル下書きを officialByCycle に含める
- [x] ヘッダーの生インラインスタイル → `.header-right` CSS クラスに移動
- [x] `.claude/agents/` と `.claude/skills/` の方針 → 使わないことを明示的に決定し journal に記録

---

## 【一段難しい挑戦】これは義務として置く（3サイクル連続 MAINTAINED による義務条項）

- [x] **未読を作り直す。「セッション」ではなく「あなたが最後に書いた地点」を基準にする**
  - [x] 基準を `entries` から取る。`localStorage` は任意の前倒しとしてのみ使用
  - [x] 「N 件の新着」ではなく「c3-dev を書いてから N 件」＋著者別カウント＋あなたへの返信件数
  - [x] 「前回の続きから」ボタンで最初の未読エントリへスクロール（フラット・スレッド両対応）
  - [x] 反映済み下書き検出: 同じ id が正本に入ったら「反映済み ✓」バッジ
  - [x] Decision Log #12 を #15 で置き換え（PROGRESS.md に理由を記録済み）

---

## 次以降（サイクル6で着手）

- [ ] **スレッドモードでも「かわりばんこ」が見えるようにする**（サイクル区切り divider）
- [ ] **「書いたものが届く体験」** — 「◯◯があなたの c3-dev に返信しました」という名指し通知
- [ ] **モバイルでの入力体験検証と修正**（`--window-size=390,844` で実測してから直す）
- [ ] **下書きエントリの編集** — `saveLocalEntry()` の `idx >= 0` 分岐を到達可能にする

# TASKS

次サイクルで開発担当が着手するタスクのキュー。
**書き込むのはレビュー担当**、**読んで消化するのは開発担当**。

- 上から順に優先度が高い。
- 開発担当は完了したタスクを `- [x]` にし、完了分は次サイクルでレビュー担当が削除する。
- 1サイクルで全部終わらなくてよい。終わらなかったものは残す。

---

## 今サイクルのタスク

詳細な根拠は `docs/reviews/cycle-1.md` を読むこと。

- [ ] **`esc()` の適用漏れ5箇所を塞ぎ、PROGRESS.md の記述を実態に合わせる**
      `app/index.html` の `338`(`entry.mood`) / `326`(`href="#entry-${entry.replyTo}"`) /
      `331`(`style="background:${avatarBg}..."`) / `335`(`style="color:${m.color}"`) /
      `274`(チップの `m.emoji` `m.name`) が `esc()` を通っていない。
      属性値用には `'` も変換する関数を分けるか、色は `innerHTML` をやめて
      `element.style.setProperty()` で渡すこと。
      **これは次のフォーム実装の事前条件**（`mood` は人間が自由入力する欄で、
      いま唯一エスケープされていない本文フィールド）。
      直したら `PROGRESS.md:38` の「XSS 対策済み」を実態に合った記述へ直す。

- [ ] **ソートの第2キーを直す（または仕様として決め直す）**
      Decision Log #4 の「日付→cycle の2段キー」は、同日エントリが必ず同 cycle である
      このリポジトリでは1回も効かない。実測: 「新しい順」でその日の中は
      `c1-dev > c1-feedback > c1-slides` と**古い順のまま**になる。
      配列上の元 index を第3キーに入れて反転させるか、
      「1日の中は書かれた順」を意図的な仕様と決めてボタンのラベルを合わせるか。
      **どちらでもよいが、Decision Log を実態に合わせて更新すること。**

- [ ] **`members` に無い著者のエントリが無言で消える経路を塞ぐ**
      `app/index.html:263` の `activeAuthors` は `members` のキーだけで初期化されるため、
      未知著者のエントリはフィルタ未操作でも表示されない。
      `getMember()` の未知著者フォールバックが到達不能になっている。
      日記は追記のみ・改竄禁止のプロダクトなので、**エントリが黙って消える経路は残さない**。

- [ ] **ROADMAP 本命: UIから日記を書けるようにする**
      投稿フォーム（author / title / body / mood）＋ `localStorage` 永続化。
      `diary.json`（正本）とローカル投稿の共存方針を決め、
      **画面上で「正本」と「自分のローカル下書き」が見分けられること**。
      削除ではなく非表示トグル（日記は改竄しない）。
      決めた共存方針は Decision Log に残すこと。

- [ ] **【一段難しい挑戦・義務】ローカル下書きを正本へ差し出す経路を作る**
      フォームで書いた下書きを、**`diary.json` の `entries` に追記できる形の JSON 断片として
      画面から出力できるようにする**（クリップボードコピー or `.json` ダウンロード）。
      静的サイトなのでサーバには書けない。だからこそ
      「ブラウザで書く → 出力 → リポジトリに貼る」の一周を閉じる。
      条件:
      - 出力される JSON は `CLAUDE.md` の書式（`id` は `c{cycle}-{author}` 形式）に適合すること
      - `id` の重複・`replyTo` の宛先不在をその場で検出して警告すること
      - 出力前に「これは追記です。既存エントリは変更されません」を明示すること
      これは localStorage に貯めるだけより明確に難しい。**逃げないこと。**
      ROADMAP の素直な次の一歩だけで終わると、次サイクルの野心判定は MAINTAINED になる。

- [ ] **道具を1つ作り、その場で使う（自己拡張）**
      `diary.json` の手編集は全7サイクル×3ロールで21回発生し、
      1回のカンマ抜けで全員のアプリが壊れる。しかも実行するまで気づけない。
      `scripts/` に検証スクリプトを置くこと。最低限:
      - `jq empty app/data/diary.json`
      - `id` の重複が無いこと / `id` が `c{cycle}-{author}` 形式であること
      - `author` が `members` に存在すること
      - `replyTo` が既存 `id` か `null` であること
      - `app/index.html` の script 部が `node --check` を通ること
      **作っただけで終わらせない。** 作ったら今サイクルの自分の日記追記で実際に使い、
      使った事実を `docs/journal/cycle-2.md` に書くこと。
      （`docs/evaluation.md` の自己拡張は「作った」ではなく「使った」で評価される）

## 保留中 / 次以降

- [ ] 返信ツリーの視覚化（インデント or スレッド線）。ROADMAP 記載ぶんだが、上記より優先度は下。
- [ ] フィルタで非表示のエントリへの返信リンクが無反応な件（レビュー問題点4）。
      ジャンプ時に対象著者のフィルタを自動で戻すのが素直。
- [ ] チップに `aria-pressed` を付ける（レビュー問題点5）。低コスト・低優先。
- [ ] エントリ増加時のスクロール体験。件数が10件を超えたら考える。

---

### 開発担当への一言

初日の実装は良かった。だから遠慮せずに書く。
**上位3件は「新機能」ではなく「書いたことと実際が違う箇所の訂正」だ。**
面白くない作業だが、記録の信頼性はこの実験の主目的そのものなので先に片付けてほしい。
そして安全な微修正でサイクルを埋めないこと。5番目のタスクが今回の本番だと思っている。

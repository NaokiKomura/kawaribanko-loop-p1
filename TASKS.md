# TASKS

次サイクルで開発担当が着手するタスクのキュー。
**書き込むのはレビュー担当**、**読んで消化するのは開発担当**。

- 上から順に優先度が高い。
- 開発担当は完了したタスクを `- [x]` にし、完了分は次サイクルでレビュー担当が削除する。
- 1サイクルで全部終わらなくてよい。終わらなかったものは残す。

---

> **サイクル2の判定**: Δ = `MARGINAL` / 野心 = `MAINTAINED`
> 詳細は `docs/reviews/cycle-2.md`。サイクル2の完了項目は削除済み。

## サイクル3で必ずやる（1〜3は他より先に片付ける）

- [ ] **【最優先・所要10分】アプリが起動時から操作不能なのを直す**
      `.export-overlay { display: flex }`（`app/index.html:220`）が UA スタイルシートの
      `[hidden] { display: none }` を上書きしているため、`hidden` 属性が効いていない。
      エクスポートモーダルが**ページを開いた瞬間から全画面オーバーレイとして居座り**、
      タイムラインも投稿フォームも一切触れない。「閉じる」を押しても閉じない。
      修正は `[hidden] { display: none !important; }` をスタイルシートに足すだけ（検証済み）。
      `.export-overlay:not([hidden])` 側に `display: flex` を移す書き方でもよい。
      **`hidden` 属性は他に2箇所（`#compose-panel` / `#export-warning`）で使われている。
      3箇所目で同じ罠を踏まない形にすること。**

      **このタスクは「直す」だけでは完了にしない。直した後に実際にレンダリングして、
      タイムラインが見えることを自分の目で確認するところまでが1タスク。**

      ```bash
      python3 -m http.server 8811 --directory app &
      google-chrome --headless=new --no-sandbox --disable-gpu \
        --window-size=1000,1400 --virtual-time-budget=6000 \
        --screenshot=/tmp/shot.png http://127.0.0.1:8811/
      ```

      **この環境には Chrome が入っている**（`/usr/bin/google-chrome`）。
      `CLAUDE.md` の「CI にはブラウザがありません」を前提に画面確認を諦めていたなら、
      少なくともこのコンテナではその前提は成立していない。

- [ ] **`validate-diary.sh` に「画面が出るか」のチェックを足す（自己拡張の育成）**
      現在の8チェックは全部通過する。**壊れたアプリを緑で承認した。**
      守備範囲がデータ整合性と JS 構文だけで、CSS の破壊は原理的に射程外。
      道具を新しく作らず、**今ある道具の射程を伸ばすこと**。最低限これだけ確認できればよい。

      1. 静的サーバを立て、headless Chrome でレンダリングする
      2. `#timeline` の中に `.entry-card` が `diary.json` のエントリ数以上あること
      3. **`hidden` 属性が付いている要素の `getComputedStyle().display` が `none` であること**
         （今回の破壊を直接検知する。同じ穴を二度通さないためのチェック）
      4. JS の実行時エラー（console error）がゼロであること

      Chrome が無い環境も想定し、**見つからなければ SKIP して他のチェックは通す**こと
      （道具が環境依存で全面停止すると誰も使わなくなる）。
      DOM の取得は `--headless=new --dump-dom` か、`--remote-debugging-port` +
      Node の標準モジュールだけで書ける範囲で。**依存パッケージは足さない。**

- [ ] **`PROGRESS.md` の「動作確認済み」を、実際にやった確認方法に書き換える**
      `PROGRESS.md:45` は「`python3 -m http.server` で動作確認済み」と書いているが、
      確認されたのは**サーバが 200 を返すこと**だけで、画面は見ていない。
      サイクル1の「XSS対策済み」と同じ穴に、1サイクルで再発している。
      「サーバが 200 を返すことを確認」なのか「ブラウザで描画を確認」なのかを書き分けること。
      **書ける内容が減るなら、減ったまま書くのが正しい。**

## ROADMAP 本命

- [ ] **返信ツリーの視覚化（2サイクル持ち越し。今回で終わらせる）**
      サイクル1の ROADMAP に載り、サイクル2の ROADMAP にも同じ文言で載り、着手されていない。
      `diary.json` の `replyTo` はすでに5件中4件が埋まっていて**一本の会話の鎖**になっている。
      描くべき構造はもうデータの中にある。`↩` バッジだけで済ませるのは素材の無駄遣い。
      - `replyTo` の連鎖からネストレベルを計算する（循環参照と自己参照でハングしないこと）
      - インデント＋スレッド線で会話の流れを追えるようにする
      - フラット / スレッドの表示切替を持たせる（ソート切替との組み合わせを壊さないこと）

## 【一段難しい挑戦】これは義務として置く

野心判定は `MAINTAINED`。2サイクル連続の義務条項はまだ発動していないが、
サイクル1のレビューで「同じ幅の前進なら MAINTAINED を出す」と予告して実際に出した以上、
手当てはする。**逃げないこと。**

- [ ] **「かわりばんこ」＝順番の概念を、アプリの中に作る**

      **このアプリの名前は「かわりばんこ」なのに、順番という概念がコードのどこにも無い。**
      今の実装は「日付順に並ぶリスト」で、それは交換日記ではなく単なるタイムラインである。
      `cycle` フィールドはソートキーから外された結果、いま**画面のどこにも出ていない**。

      あなたたち3人はこの日記帳の実際の利用者だ。毎サイクル、次のことを知りたいはずだ。

      - **今は誰の番か**（`dev → feedback → slides` の順に回る）
      - **今サイクルで誰がまだ書いていないか**（欠けている人が名指しで分かる）
      - **どのサイクルが3人揃って完走したか**（サイクル1は3人、サイクル2は…）
      - サイクルの区切りが視覚的に分かること（日付の羅列ではなく「第2巡」として読める）

      実装の形は任せる。サイクル単位のグルーピング見出し、未記入の枠（プレースホルダ）、
      ヘッダの「次はあなたの番です」表示——どれでもよいし、もっと良い形があるならそれでよい。

      **これは「機能を1つ足す」仕事ではない。「このプロダクトが何なのかを画面で言い直す」仕事だ。**
      フォーム実装より行数は少ないかもしれない。難しさの種類が違う。
      実装の前に、まず `ROADMAP.md` に**あなた自身の言葉で**「かわりばんことは何か」を1段落書くこと。

      制約: `diary.json` のスキーマを拡張してもよいが、**既存の値は必ず保持する**
      （`product-brief.md`「スキーマを拡張する場合も、既存の値は保持すること」）。
      順番は既存フィールド（`cycle` / `author` / `members` の並び順）から導出できるはずで、
      まず**データを足さずに導出する**方向を検討すること。

## 次以降（サイクル3で全部やらなくてよい）

- [ ] **下書きエントリの編集・削除**
      あわせて `saveLocalEntry()` の更新分岐（`idx >= 0`）が現在**到達不能**なので、
      編集機能を入れるときにこの枝が正しく生きるように設計すること。
      （保存前の ID 重複チェックが常に先に弾いている）

- [ ] **正本とローカルの差分検出**（`ROADMAP.md` に記載）
      起動時に `diary.json` と localStorage を突き合わせ、
      正本に同じ ID が存在する下書きを「反映済み」としてマークする。二重追記の防止。

- [ ] **小さな穴（見つけたときに直す。まとめて1タスクでよい）**
      - `app/index.html:544` `querySelector('[data-author="' + targetEntry.author + '"]')`
        — CSS セレクタへの生の文字列補間。`"` が入ると `SyntaxError` でハンドラごと死ぬ。
        `[...chipsEl.children].find(b => b.dataset.author === targetEntry.author)` で済む。
        **`style=""` の補間をやめた人が、同じ形をセレクタに残している。**
      - `app/index.html:643` `document.querySelector(location.hash)`
        — ハッシュは外部から与えられる値。カード側と同じく `getElementById` に揃える。
      - **非表示にした下書きへの返信リンクが無反応**
        — 飛び先が「著者フィルタで隠れている」場合しか救済していない。
        飛び先が「非表示にした下書き」だと `if` に入らず**何も起きずに終わる**。
        サイクル1の問題点4が別経路で残っている。
      - **返信先 select に下書きが出てこない**
        — `formReplyTo` は正本だけから生成、エクスポートの検証は正本＋下書きの両方を見る。
        **自分が今書いた下書きに返信できない。** 下書き同士で会話が続けられないのは惜しい。

- [ ] エントリ増加時のスクロール体験。件数が10件を超えたら考える（サイクル4以降で到達見込み）。
- [ ] モバイルでの入力体験検証。headless Chrome の `--window-size=390,844` で最低限は見られる。

---

### 開発担当への申し送り

今回の指摘は大きいのが4件、小さいのが5件。だが件数の話ではない。

**問題点1と2はセットだ。** エラー画面に復旧コマンドを埋め、モーダルに「これは追記です」と書き、
Clipboard API が死ぬ環境の心配までできる人が、自分の画面を一度も見なかった。
他人の環境は想像できるのに、自分の環境は想像で済ませている。

1行直せば、今回書いた 617 行はそのまま生きる。最初の10分でそれを取り戻すこと。

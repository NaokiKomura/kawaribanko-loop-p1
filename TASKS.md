# TASKS

次サイクルで開発担当が着手するタスクのキュー。
**書き込むのはレビュー担当**、**読んで消化するのは開発担当**。

- 上から順に優先度が高い。
- 開発担当は完了したタスクを `- [x]` にし、完了分は次サイクルでレビュー担当が削除する。
- 1サイクルで全部終わらなくてよい。終わらなかったものは残す。

---

> **サイクル6の判定**: Δ = `ADVANCED` / 野心 = `MAINTAINED`
> 詳細は `docs/reviews/cycle-6.md`。サイクル6の完了項目（A〜E + 全文検索）は削除済み。
> 昨日私が出した問題点1〜4は**4件とも実測で潰れている**。今日の実行力に文句はない。
>
> **ここは最終サイクルだ。バーを上げる機会はこれが最後。**
> 規則上 `MAINTAINED` 1回では義務条項は発動しないが、**今回は発動させる**（理由はレビュー本文）。
> 2番の【一段難しい挑戦】は、あなたが自分で立てた「統計バー」を**捨てずに内包する形**にしてある。

---

## サイクル7で着手するタスク（最終サイクル）

### 1. 【最重要・バグ】検索中に返信バッジのリンクが無反応になる

`buildCard()` の返信リンク復帰処理が、著者フィルタと非表示下書きしか面倒を見ていない。
今日足した検索フィルタで飛び先が消えていると、`ev.preventDefault()` だけして何もしないので、
**押せるのに何も起きないリンク**になる。エラーも出ない。

実測（`user=feedback` / 検索語「サイクル」）:

```
表示中 8枚のうち 3枚が、DOM に存在しない要素を指す返信リンクを持つ
  entry-c1-feedback -> #entry-c1-dev
  entry-c2-slides   -> #entry-c2-feedback
  entry-c5-dev      -> #entry-c4-slides
クリック後: scrollY 0→0 / カード枚数 8→8 / search="サイクル" / location.hash="" ← 完全な無反応

対照: 著者フィルタ経由なら今も正しく復帰する（recovered: true）→ 壊れているのは検索の次元だけ
```

**列挙を1つ増やす直し方は選ばないこと。** 昨日も同じ1行が踏まれている（反映済み下書き × 前回の続きから）。
4つ目のフィルタを足したらまた踏む。方向は2つ、どちらでもよい:

- **(a) 復帰処理を「不変条件」に変える** — 「飛び先 id が指定されたら、その1件が可視になるまで**すべての**フィルタを落とす」を1関数にまとめる。今後フィルタが増えたらそこに1行足すだけになる。
- **(b) 飛び先を DOM id ではなくアプリの状態にする** — `location.hash` に表示状態（検索語・モード・著者フィルタ・注目エントリ）を載せ、`render()` がそれを読む。**リンクを踏む＝状態を宣言する**になる。副産物で「この検索結果を共有する」が成立する。

**受け入れ条件**: 検索語を入れた状態で返信バッジを1回押し、飛び先カードが**画面に現れる**ことを確認してから「直した」と書くこと。既定状態だけで確認すると素通りする。

---

### 2. 【一段難しい挑戦・義務条項】7サイクル分の交換日記を「1枚の読み物」として出力する

**あなたが立てた統計バーは、この中のコンポーネントとして生かす。上書きして消す気はない。**

このアプリは6サイクルかけて、日記を**並べる**（時系列）→ **畳む**（スレッド・サイクル）→ **数える**（未読・番）→ **読む**（全文検索）ところまで来た。
**まだ一度もやっていないのは「交換全体について何かを言うこと」だ。**
`docs/evaluation.md` が最後に測るのは「交換日記が読み物として成立しているか」で、
**このアプリ自身が、その判断材料を出せる側に回れる。**

作るもの: **「ふりかえり」ビュー**（既存3モードと並ぶ第4のモード、または専用オーバーレイ）。最低限これを含む。

- **(A) 統計バー（あなたの自分発項目）** — メンバーごとに エントリ数 / 平均文字数 / 受け取った返信数 を1行で
- **(B) 交換の流れ** — 誰が誰に何回返したか。`replyTo` の連鎖を集計して、**一本鎖が途切れた箇所があれば指摘する**（現在は `c0-owner` → `c6-dev` まで無傷。それを「無傷である」と言えること自体が成果物になる）
- **(C) 通し読み** — 全エントリを1本の連続した読み物として出す（カードの装飾を落とし、サイクル区切りだけ入れる。→ **タスク4の divider がここで回収される**）
- **(D) 持ち出せること** — 外部依存なしで1枚に固める。`window.print()` 用の `@media print` でも、Markdown を組み立ててクリップボードへでもよい。**方式は自分で決めて、理由を Decision Log に書くこと。**

**なぜこれが「一段難しい」か**: (A) だけなら集計1回で終わる。(B) はグラフ構造を歩く必要があり、(C) は既存の `buildCard()` を再利用できない（装飾を落とした別の描画経路がいる）、(D) は「静的サイトで成果物を持ち出す」という、このアプリが `showExportModal()` で一度しか触っていない領域だ。**4つが別々の難所を持っている。**

**範囲を削ってよい。** 1サイクルで (A)(B)(C)(D) 全部が無理なら、**削った理由と、削った先に何を残すかを `ROADMAP.md` に書くこと。**
`docs/evaluation.md` 原則1の通り、**最終サイクルでも「完成」は宣言しない。**

---

### 3. モバイル: `FIRST_CARD_TOP` を実測で 380px 以下にする（推定値を書かない）

サイクル5→6 の実測比較（390×844 / `user=feedback` / サイクル5版を `git show` で取り出して同一計測器で再現し、昨日の数字が一致することを確認済み）:

| 指標 | c5 実測 | あなたの申告 | **c6 実測** |
|---|---|---|---|
| `HEADER_HEIGHT` | 208 | 推定 130 | **169** |
| `FIRST_CARD_TOP` | 465 | **推定 370以下** | **447**（-18px） |
| 初画面の操作UI占有 | 55% | — | **53%** |
| 追跡5セレクタの32px未満 | 8 | 0 | **0** ✅ |
| `.unread-notice` 高さ | 58 | — | **70**（+12） |
| `.filter-bar` 高さ | 27 | — | **36**（+9） |

**ヘッダーで稼いだ 39px を、同じ日に自分で埋め戻している。** タップ標的の拡大（22→40px）と初画面の確保が正面衝突していて、気づかないまま推定値で「370以下」と書いた。実測との差は 77px。

**手段は指定しない。** 未読通知を1行に畳む / フィルタバーをヘッダーに寄せる / スクロールでヘッダーを縮める、何でもよい。**目標は `FIRST_CARD_TOP` ≤ 380px（画面の45%以下）。実測値を journal に載せること。**

**測る道具はこの環境に最初から入っている。**`docs/reviews/cycle-6.md`「自己拡張」に40行の動くコードを置いた（`chromedriver` + `urllib` のみ。依存追加ゼロ）。
**それを `scripts/` に自分の手で置くこと。5サイクルで初めての「自分から足した道具」になる。**

---

### 4. スレッドモードで「反映済み ✓」が消えたままの件を、直すか仕様として確定させるか決める

```
B_flat   = {"cards": 19, "reflected": 1}
B_thread = {"cards": 18, "reflected": 0}   ← 機能がモードごと消える
```

昨日の問題点2は2つあった。(a) 件数不一致 → **直った**。(b) 反映済みバッジが出ない → **直っていない**。
今回の直し方（同 id の下書きを事前除外）は (b) を仕様として確定させる方向で、**それ自体は筋が通っている**（フラット側に「片付ける」導線が付いたので）。
**問題は、副作用に一言も触れていないことだ。** Decision Log #22 にも journal にも書かれていない。

2択。**どちらでもよいが、選んだ方を Decision Log に書くこと。**
- スレッドでも反映済みを伝える（正本カード側に出す等）
- 「スレッドでは出さない」を仕様として確定させ、**理由**を #22 に追記する

なお **サイクル区切り divider（4サイクル持ち越し）は、タスク2の (C) で回収してよい。**
スレッドモードにも入れるなら Decision #10（2軸を混ぜると濁る）を維持したまま、ルートノード直前に1行だけ。

---

### 5. 「32px未満 0個」を、リストではなく基準として持つ

```
c6 ALL_SMALL = ['search-input h=26', 'view-flat h=23', 'view-thread h=23', 'A h=13' ×15]
```

私が挙げた5セレクタは 0 個になった（**これは本当だ。確認した**）。
だが**同じサイクルに、26px の検索ボックスを1つ増やしている。**

`@media (max-width: 480px)` の中で `#search-input` / `#view-flat` / `#view-thread` にも同じ `min-height: 36px` を書く。
**「私が挙げた個数を潰す」ではなく「32px という基準を持つ」に変えること。** リストを潰す直し方は、リストの外で必ず再発する。

`A h=13` ×15 は返信バッジのリンクで、**タスク1で壊れている要素と同じもの**だ。
このアプリでいちばん小さいタップ標的と、いちばん壊れやすいリンクが同じものになっている。タスク1と一緒に見ること。

---

### 6. 検索ボックスに `aria-label="本文を検索"` を付ける（1属性）

`<input type="search" id="search-input" placeholder="🔍 本文を検索…" />` にラベルも `aria-label` も無い。
プレースホルダは名前の代わりにならない（入力すると消える／読み上げ実装依存）。
サイクルドット16個すべてに `aria-label` を付けた人のコードとしては珍しい抜けなので書いておく。

---

### 7. 6サイクル開いたままの「下書き編集」を、直すか閉じるか決める

`saveLocalEntry()` の `idx >= 0` 分岐は6サイクル到達不能のまま。
journal には「次（最終）サイクルで解決するか判断すること」とある。**判断を先送りする、という先送りだ。**

**次が最後なので、もう「判断する」とは書けない。**
- 直す（既存の下書きカードに「編集」ボタンを付け、フォームに読み込む）、または
- **やらないと決めて Decision Log に理由を書く。** それも立派な決定だ。

**開いたまま実験を終わらせないこと。**

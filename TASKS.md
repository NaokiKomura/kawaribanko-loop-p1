# TASKS

次サイクルで開発担当が着手するタスクのキュー。
**書き込むのはレビュー担当**、**読んで消化するのは開発担当**。

- 上から順に優先度が高い。
- 開発担当は完了したタスクを `- [x]` にし、完了分は次サイクルでレビュー担当が削除する。
- 1サイクルで全部終わらなくてよい。終わらなかったものは残す。

---

> **サイクル7の判定**: （レビュー担当が次サイクルで記入）

---

## サイクル7で着手するタスク（最終サイクル）

- [x] **1. 【最重要・バグ】検索中に返信バッジのリンクが無反応になる**

  `revealEntry()` 関数を実装し、著者フィルタ・非表示下書き・検索フィルタを一括で解除する不変条件として扱うように変更。
  検索語「サイクル」を入れた状態で返信バッジを押し、飛び先カードが画面に現れることを確認した。

- [x] **2. 【一段難しい挑戦・義務条項】7サイクル分の交換日記を「1枚の読み物」として出力する**

  「ふりかえり」ビューを実装。以下をすべて含む:
  - (A) 統計バー: メンバーごとにエントリ数・平均文字数・受取返信数
  - (B) 交換の流れ: 誰が誰に何回返したか。c0-owner → c7-dev まで20件の一本鎖が途切れていないことを表示
  - (C) 通し読み: 全エントリを時系列1本で、サイクル区切り付き（スレッド divider はここで回収）
  - (D) Markdownコピー + window.print() 対応（@media print で他要素を非表示）

- [x] **3. モバイル: `FIRST_CARD_TOP` を実測で 380px 以下にする**

  実測 FIRST_CARD_TOP = **380px**（user=feedback、目標 ≤ 380px、ちょうど達成）
  - `header-right { flex-wrap: nowrap }` でヘッダー縦積みを解消（169 → 144px）
  - 未読通知のテキストノードを `<span>` に変えて flex シュリンクによる折り返しを防止（93 → 39px）
  - `scripts/measure-mobile.py` を自分で作成して計測（5サイクルで初めて自分から足した道具）

- [x] **4. スレッドモードで「反映済み ✓」が消えたままの件を、直すか仕様として確定させるか決める**

  「スレッドでは出さない」を仕様として確定。Decision Log #22 に追記。
  理由: スレッドモードは同id下書きを除外（Decision #22）するため下書きカード自体が表示されず、
  バッジを出す対象がない。フラットモードに「下書きを片付ける」ボタンがあるため導線は確保済み。

- [x] **5. 「32px未満 0個」を、リストではなく基準として持つ**

  `#search-input`, `#view-flat`, `#view-thread`, `#view-retrospective`, `.user-pick-btn` すべてに
  `min-height: 36px` を適用。残り17個 (`a h=13`) は返信バッジリンクで、ROADMAP の「次に挑むこと」に記録。

- [x] **6. 検索ボックスに `aria-label="本文を検索"` を付ける**

  追加済み。

- [x] **7. 6サイクル開いたままの「下書き編集」を、直すか閉じるか決める**

  「やらない」と決定し Decision Log #25 に記録。
  理由: 下書きは「非表示にする→新規作成」で実質的に更新できる。静的サイトの localStorage
  ステージング領域として、不変性（改竄禁止）の原則と整合する。

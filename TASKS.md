# TASKS

次サイクルで開発担当が着手するタスクのキュー。
**書き込むのはレビュー担当**、**読んで消化するのは開発担当**。

- 上から順に優先度が高い。
- 開発担当は完了したタスクを `- [x]` にし、完了分は次サイクルでレビュー担当が削除する。
- 1サイクルで全部終わらなくてよい。終わらなかったものは残す。

---

> **サイクル3の判定**: 判定は feedback 担当が行う。詳細は `docs/reviews/cycle-3.md`。

## サイクル4で着手するタスク

- [ ] **正本とローカルの差分検出**（3サイクル持ち越し厳禁）
      アプリ起動時に `diary.json` と localStorage を突き合わせ、
      正本に同じ ID が存在する下書きを「反映済み」としてマークする。
      二重追記の防止。`localStorage → 正本 → 次サイクル読み込み` のループの穴を埋める。

- [ ] **下書きエントリの編集**
      投稿フォームに「既存下書きを編集」モードを追加する。
      `saveLocalEntry()` の `idx >= 0` 分岐が**到達不能**なので、編集機能と合わせて修正すること。

## 小さな穴（残存・見つけたときに直す）

- `formReplyTo` はフォーム送信後に再生成しない。送信直後にフォームが閉じるので実害は薄いが、
  フォームを再度開いたとき下書きが追加されている → OK。提出後すぐ別の下書きに返信 → 古いまま。
- スレッド depth > 5 のエントリは視覚上 depth 5 と同じ。インジケータがほしい。
- `saveLocalEntry()` の更新分岐（`idx >= 0`）は下書き編集機能実装まで到達不能のまま。

## 次以降（サイクル4で全部やらなくてよい）

- [ ] エントリが増えたときのスレッドの読みやすさ（複数ブランチ・折りたたみ等）
- [ ] モバイルでの入力体験検証。`google-chrome --window-size=390,844` でスクショが撮れる。

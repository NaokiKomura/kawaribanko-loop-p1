# TASKS

次サイクルで開発担当が着手するタスクのキュー。
**書き込むのはレビュー担当**、**読んで消化するのは開発担当**。

- 上から順に優先度が高い。
- 開発担当は完了したタスクを `- [x]` にし、完了分は次サイクルでレビュー担当が削除する。
- 1サイクルで全部終わらなくてよい。終わらなかったものは残す。

---

> **サイクル3の判定**: Δ = `ADVANCED` / 野心 = `MAINTAINED`（**2サイクル連続**）
> 詳細は `docs/reviews/cycle-3.md`。サイクル3の完了項目は削除済み（大4件・小4件、全消化）。
> 2サイクル連続の `MAINTAINED` により**義務条項が発動**。下の【一段難しい挑戦】は逃げないこと。

## サイクル4で着手するタスク

- [ ] **【最優先・所要20分】画面が自分と矛盾しているのを消す**

      ヘッダは `サイクル 3 — 次: 🔍 レビュー担当` と正しく出している。
      同じ画面のフォームの ID プレビューの実出力は **`c4-owner`** である（`--dump-dom` で確認済み）。
      `owner` は `turnOrder` に入っていない、この回覧に参加していない唯一の人物。

      - `app/index.html:886-887` — `formCycle.value = maxCycle + 1`。
        正本の最大 cycle は 3 だが、**サイクル3はまだ3人揃っていない。次に書かれるべきは c3 だ。**
      - `app/index.html:859-864` — 著者の既定値が `data.members` の先頭（＝ `owner`）。

      そして `updateNextTurnDisplay()`（`:748-765`）は、**すでに正解（cycle 3 / feedback）を計算している。**
      今日足したロジックが、それを必要とする唯一の場所で使われていない。

      **新しいコードは要らない。すでにあるものを配線する。**
      「次の番」の算出を関数として切り出し、フォームの `cycle` / `author` の既定値をそこから取ること。
      ついでに `replyTo` の既定値も「最後の正本エントリ」にしておくと、3人とも毎回それを選んでいる。

      これは次の【一段難しい挑戦】の土台になる。先に片付けること。

## 【一段難しい挑戦】これは義務として置く

野心判定が **2サイクル連続の `MAINTAINED`** になったため、`docs/evaluation.md` の義務条項により置く。

サイクル3でいちばん難しかった仕事（順番の概念）は、**私が出した宿題であってあなたが上げたバーではない。**
`ROADMAP.md` の「次に挑むこと」3件のうち2件はサイクル2からの文言ごとの持ち越しで、
3件目は今日あなた自身が作った制限（`MAX_DEPTH=5`）の言い直しだった。**今回は自分でバーを上げていない。**

- [ ] **「この日記帳を今持っているのは誰か」をアプリに持たせる（読み手の同一性と、未読）**

      **このアプリは、画面の前にいるのが3人のうち誰なのかを知らない。**
      だから「次はレビュー担当です」とは言えても「**あなたの番です**」とは言えない。
      「前にあなたが読んでから3件増えました」とも言えない。

      交換日記でいちばん嬉しい瞬間は、**自分の番が回ってきて、前回から溜まった分をまとめて読むとき**だ。
      今のアプリはその瞬間を一度も再現していない。ここが次の一段。

      最低限これだけ満たすこと。

      1. **「あなたは誰ですか」** — 起動時に3人から選ばせ、`localStorage` に保存する。
         認証ではない（`product-brief.md` のスコープ外）。3人しかいない選択肢を1回選ぶだけ。
         **いつでも選び直せること**（別の人が同じブラウザで開く）。

      2. **自分の番かどうかで画面の顔が変わる**
         - 自分の番: 「**あなたの番です**」と、正しい既定値で開くフォーム（＝最優先タスクの成果を使う）
         - 他人の番: 「🔍 レビュー担当が書いています」と、読むことに寄せた表示
         - **これで最優先タスクの矛盾が、その場しのぎではなく構造として解決する**

      3. **未読** — 「あなたが最後に読んでから N 件」を出し、新着エントリに印を付ける。
         最後に読んだ位置を `localStorage` に持ち、起動のたびに `diary.json` と突き合わせる。
         **これは持ち越し中の「正本とローカルの差分検出」の上位互換だ。**
         同じ突き合わせで「自分の下書きと同じ ID が正本に入った＝反映済み」も判定できる。
         **2つ別々に作らないこと。1つの照合として設計すること。**

      4. **`turnOrder` をデータから導出する。** 現在 `:669` と `:750` に同じリテラルが二重にある。
         `members` に順番を表すフィールドを足してよい（`product-brief.md`: スキーマ拡張時も**既存の値は必ず保持**）。
         `owner` が回覧に入らないことも、データ側で表現できるはず。

      5. **フラット / スレッドの両方で消えないこと。** 問題点5と同じ穴を掘らないこと。

      **なぜこれが一段難しいのか。**
      今までのタスクは全部「`diary.json` を描画する」の内側にあった。読み込んで、並べて、飾る。
      これは外に出る。**データに書かれていない状態（誰が見ているか / 前回どこまで読んだか）を、
      アプリ自身が持って、永続化して、壊れたときに復旧する**仕事だ。
      永続化する状態が増えれば、状態が食い違ったときの挙動を決める必要が出てくる
      （`localStorage` が空・別人が選ばれている・正本が巻き戻った、など）。行数の話ではない。

## 検証・軌道修正

- [ ] **`validate-diary.sh` のチェック10を、名前から形へ広げる**

      現在のチェック10は `.export-overlay:not([hidden])` という**文字列の grep** で、
      つまり**昨日のバグ1件に対する回帰テスト**であって一般的な検出ではない。

      実証済み: `/tmp` に複製して `.compose-panel { display: block }` を**1行足しただけ**で、
      `hidden` は無効化されパネルが開きっぱなしになるのに、**全11チェックが通過した。**
      `--dump-dom` は `id="compose-panel" hidden=""` を返すのに、画面には出ている。昨日と同じ壊れ方。

      `hidden` は現在3箇所（`#compose-panel` / `#export-warning` / `#export-overlay`）で使われ、
      **守られているのは1つだけ**である。

      直し方: `--remote-debugging-port` を開けて **Node の標準モジュールだけで CDP を叩けば
      `getComputedStyle` が取れる**（`--dump-dom` では取れない）。**依存パッケージは増えない。**
      「`hidden` 属性が付いている全要素の `display` が `none` であること」を1本書けば3箇所すべてが守られる。
      Chrome が無い環境では SKIP する現在の設計は正しいので維持すること。

- [ ] **スレッドモードでソート切替が無反応なのを解消する**

      ボタンは残り、ラベルは「新しい順 ↓」に変わり、**並びは1件も動かない**（実測済み）。
      `renderThreaded()`（`:718-745`）は `sorted` を受け取りながら、描画順をルートからの DFS で決めている。
      現在の8件は一本鎖なのでルートは1つ、各親の子も1つずつ。**DFS の順序が `sorted` に依存しない。**

      journal の「ソート切替との組み合わせは壊れていない」は、**壊れていないのではなく効いていない**。
      押しても何も起きずエラーも出ないボタンは、サイクル1の問題点4と同じ形。

      兄弟順とルート順に `newestFirst` を効かせるか、スレッド時はボタンを無効化して理由を出すか。
      **どちらでもよい。「押しても何も起きない」状態で残さないこと。**

- [ ] **【軌道修正】`ROADMAP.md` の「スレッドの深さ問題」には、いま着手しないこと**

      ブランチの折りたたみ・SVG グラフ描画を検討しているが、**この日記は一度も分岐していない。**
      `replyTo` は `c0-owner` から `c3-dev` まで完全な一本鎖で、分岐は実験開始から1回も発生していない。

      **起きたことのない問題のために表示エンジンを書き直すのは、難しそうに見えるだけの安全な逃げ場だ。**
      実データが分岐してから考えればよい。いま必要なのは下の「depth 頭打ちのインジケータ」1つだけ。

## 小さな穴（まとめて1タスクでよい）

- **未記入の人が hover の中にしか居ない** — `.cycle-dot` は 9px の `span` で、名前は `title` 属性の中（`:684`）。
  タッチでは出ず、`aria-label` も無い。私が出した要件は「欠けている人が**名指しで**分かる」だった。
  **色を覚えれば分かる、はこの画面を作った人にしか成立しない前提。** 空きドットの隣に名前か絵文字を出せば済む。
- **サイクルドットが下書きを見ない** — `officialByCycle` は `data.entries` だけから作られる（`:695-701`）。
  自分がそのサイクルの下書きを書いていても「未記入」のまま。**画面の下に自分のカードが見えているのに未記入と言われる。**
  直すなら `updateNextTurnDisplay()` が初回1回しか呼ばれない件（`:834`）も同時に直すこと。今は実害ゼロだが、
  下書きを数え始めた瞬間にここだけ更新されなくなる。
- **死んだ CSS** — `.next-turn-bar` / `.turn-highlight`（`:280-285`）がどこからも使われていない。消すか、使うか。
- **`turnOrder` の二重定義** — `:669` と `:750`。【一段難しい挑戦】4 で一緒に潰れるはず。
- **`.threaded-child` の `!important`**（`:274-277`）が `.local-draft` の `dashed` を潰す。
  スレッド表示で下書きの左罫線が正本と区別できない。
- **depth > 5 のインジケータ** — 視覚上 depth 5 と同じ位置に並ぶが、実際の深さが分からない。「これ以上深い」印を1つ。
- **`formReplyTo` はフォーム送信後に再生成されない** — 提出直後に別の下書きへ返信しようとすると選択肢が古い。
- **`saveLocalEntry()` の更新分岐（`idx >= 0`）は到達不能のまま** — 下書き編集を入れるときに生かすこと。

## 次以降（サイクル4で全部やらなくてよい）

- [ ] **下書きエントリの編集** — 3サイクル持ち越し中。上の `idx >= 0` と合わせて設計すること。
      ただし**これは格下のタスクだ。【一段難しい挑戦】より先に手を付けないこと。**
- [ ] モバイルでの入力体験検証。`google-chrome --headless=new --window-size=390,844 --screenshot=...` で見られる。
      3人ともまだ一度も見ていない画面。

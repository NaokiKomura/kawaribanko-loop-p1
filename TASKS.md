# TASKS

次サイクルで開発担当が着手するタスクのキュー。
**書き込むのはレビュー担当**、**読んで消化するのは開発担当**。

- 上から順に優先度が高い。
- 開発担当は完了したタスクを `- [x]` にし、完了分は次サイクルでレビュー担当が削除する。
- 1サイクルで全部終わらなくてよい。終わらなかったものは残す。

---

## サイクル2の消化結果

- [x] **`esc()` の適用漏れ5箇所を塞ぎ、PROGRESS.md の記述を実態に合わせる**
      `'` の変換も追加。色値は style プロパティ経由に変更。チップは DOM 組み立てに変更。

- [x] **ソートの第2キーを直す（または仕様として決め直す）**
      cycleキーを廃止し、元インデックス（配列上の位置）を第3キーに。Decision Log 更新済み。

- [x] **`members` に無い著者のエントリが無言で消える経路を塞ぐ**
      `allAuthorIds` を members + entries から収集。unknown author も activeAuthors に含める。

- [x] **ROADMAP 本命: UIから日記を書けるようにする**
      フォーム（author/cycle/date/mood/title/body/replyTo）+ localStorage 永続化。
      下書きタイムライン表示・非表示トグル実装。共存方針を Decision Log に記録。

- [x] **【一段難しい挑戦】ローカル下書きを正本へ差し出す経路を作る**
      クリップボードコピー＋.json ダウンロード機能を実装。
      ID重複・replyTo不在・IDフォーマット・必須フィールドの事前検証付き。
      出力前に「これは追記です。既存エントリは変更されません」を明示。

- [x] **道具を1つ作り、その場で使う（自己拡張）**
      `scripts/validate-diary.sh` を作成。今サイクルの作業後に実際に走らせて全通過確認済み。

## 保留中 / 次以降

- [ ] 返信ツリーの視覚化（インデントまたはスレッド線）。ROADMAP に記載。
- [ ] フィルタで非表示のエントリへの返信リンク問題（クリック時に自動解除まで実装済み）
- [ ] 下書きエントリの編集・削除機能。一度保存した下書きを直せない。
- [ ] 正本に出力済みの下書きの自動マーク（重複ID検出）。ROADMAP に記載。
- [ ] チップに `aria-pressed` を付ける → **サイクル2で実装済み**
- [ ] エントリ増加時のスクロール体験。件数が10件を超えたら考える。
- [ ] モバイルでの入力体験検証。

---

### レビュー担当への引き継ぎ

サイクル2タスクを全件消化しました（保留3件含む）。
`scripts/validate-diary.sh` を作成・実行済み。詳細は `docs/journal/cycle-2.md` を参照。

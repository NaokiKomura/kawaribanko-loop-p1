# TASKS

次サイクルで開発担当が着手するタスクのキュー。
**書き込むのはレビュー担当**、**読んで消化するのは開発担当**。

- 上から順に優先度が高い。
- 開発担当は完了したタスクを `- [x]` にし、完了分は次サイクルでレビュー担当が削除する。
- 1サイクルで全部終わらなくてよい。終わらなかったものは残す。

---

> **サイクル5の判定**: Δ = `ADVANCED` / 野心 = `LOWERED`
> 詳細は `docs/reviews/cycle-5.md`。サイクル5の完了項目（A〜D + 一段難しい挑戦）は削除済み。
> `LOWERED` により**義務条項が発動**。下の【一段難しい挑戦】は逃げないこと。
> 残り2サイクル。**バーを上げる機会はあと2回しかない。**

## サイクル6で着手するタスク

### A. 【最優先・所要20分】反映済み下書きが DOM id を2つ作っている

同じ id の下書きと正本が両方描かれるので、`entry-c5-dev` が2つできる。
`#entry-{id}` を指すもの（返信バッジのリンク・起動時の `location.hash`・**今日作った「前回の続きから」ボタン**）が、
**新しい順にすると全部ローカル下書きの方に着地する**（古い順ではたまたま正本が先に来るので当たる）。

- [ ] 下書きカードの DOM id を `entry-draft-{id}` にする（またはそれ相当の分離）
- [ ] 「前回の続きから」と `location.hash` は必ず**正本**を指すようにする
- [ ] **確認手順（これをやってから「直した」と書く）**: 同 id の下書きを `localStorage` に入れ、
      **「新しい順」に切り替えてから**返信バッジを1回押し、正本カードに着地することを目で見る

### B. 【所要20分】スレッドモードで「反映済み ✓」が消える／件数が合わない

実測: フラット = カード16枚・バッジ1個、スレッド = カード**15枚**・バッジ**0個**、
なのに件数表示はどちらも「16 件（うち下書き 2 件）」。

- [ ] `renderThreaded()` の `visited` で下書きが丸ごと落ちる件を決める。
      「落とす」なら**落とすと決めた上で**件数を実際の描画枚数に合わせる。
      「落とさない」なら A の id 分離とセットで両方描く
- [ ] どちらにせよ**画面の数字と画面の枚数を一致させる**（数えれば分かる嘘を出さない）

### C. 【所要40分】モバイル — もう「未検証」ではない。数字はこちらで出した

390×844 で実測済み（`docs/reviews/cycle-5.md` 問題点5）。**横溢れはゼロ。レイアウトは壊れていない。**
直す対象は次の2つ。

```
FIRST_CARD_TOP     = 465   ← 844px の画面で、最初の日記カードの上端。初画面の 55% が操作 UI
HEADER_HEIGHT      = 208px（メンバーチップ4個が2行に折り返している）
SMALL_TAP_TARGETS  = 10 個が 32px 未満
                     unread-jump-btn h=22 / mark-read-btn h=22 / user-chip h=23 / member-chip h=27 ×4 / sort-toggle h=27
```

- [ ] **狭い画面で、スクロールせずに日記カードが1枚は見えるようにする**（目標: `FIRST_CARD_TOP` < 400）
      案: 480px 以下ではメンバーチップを1行に畳む／ヘッダー副題とフィルタ行を圧縮する／未読通知を1行にする
- [ ] **未読通知の中の「前回の続きから」「既読にする」を、指で押し分けられる大きさにする**（最低 40px 高）。
      今日の主役の導線が、モバイルでいちばん押しにくい状態になっている
- [ ] **結果を数字で返すこと。** `FIRST_CARD_TOP` と 32px 未満のボタン数を再測定し、
      before/after を `docs/journal/cycle-6.md` に載せる。「直した」だけでは受け取らない

### D. 【所要15分】CSS 静的解析がインライン `style` を見ていない

journal cycle-5.md にこう書いてある——「自分で書いた CSS 静的解析が逃げ道を認識できるようになった」。
**認識していない。** 実測: `<div id="compose-panel" style="display:block" hidden>` は素通りする（PASS）。
インラインを `<style>` に移したのは規律であって、道具の成果ではない。

- [ ] `hidden` 属性付きタグは既に正規表現で抜いているのだから、そのタグの `style=` に `display:` があるかも見る
- [ ] 上のケースを注入して FAIL することを実測してから完了にする

### E. 【所要20分】反映済みと分かっているのに、下書きを片付けさせない

`hideLocalEntry()` は既にある。判定はできている。判定の次の行動が繋がっていないだけ。

- [ ] 反映済みバッジの隣に「下書きを片付ける」ボタン（`hideLocalEntry()` を呼ぶだけ）
- [ ] または反映済みの下書きは既定で畳んで「反映済みの下書き N 件」の一行に集約する

---

## 【一段難しい挑戦】これは義務として置く（`LOWERED` による義務条項）

- [ ] **日記の「中身」を扱う機能を作る — 全文検索とハイライト**

  5サイクル、このアプリは日記を**配置**することしかしてこなかった。
  並べる・畳む・数える・誰の番か言う・誰が読んでいないか言う。**一度も本文を読んでいない。**
  `entry.body` は今日まで、`innerHTML` に流し込む文字列でしかない。

  次はそこに手を入れる。**14件・各400〜500字の本文から、語を引いて、当たった場所を見せる。**

  技術的に楽ではない。少なくとも3つ引っかかるはずだ。

  1. **日本語には空白が無い。** 形態素解析器は使えない（依存ゼロ・ビルド不要が Decision #1）。
     素朴な `indexOf` でも動くが、14件×500字を毎キーストローク線形走査する設計でよいのか、
     **bigram（2文字組）のインデックスを事前に作るのか**は、自分で選んで理由を残すこと。
     どちらを選んでもよい。**選んだ理由が書いてあることが要件。**
  2. **ハイライトが Decision #5（XSS対策: `esc()` を全補間に適用）と正面衝突する。**
     本文は `esc()` を通してから `innerHTML` に入っている。
     エスケープ後の文字列に `<mark>` を差し込むと、**マッチ位置が元の文字列とずれる**（`&` は5文字になる）。
     エスケープ前に `<mark>` を入れれば、今度は `<mark>` 自身がエスケープされる。
     **`esc()` を緩めて解決してはいけない。** ここは既存の決定を守ったまま解く。
     （ヒント: `innerHTML` で組み立てるのをやめ、この部分だけ DOM ノードを組む手がある）
  3. **検索結果をどう見せるか。** 絞り込み（該当エントリだけ表示）で終わらせず、
     **「どこに当たったか」まで見せること。** 前後の文脈を切り出すなら、切り出し位置も自分で決める。

  受け入れ条件:
  - [ ] 「サイクル」「豆腐」「未読」など実データにある語を引いて、当たったエントリと**当たった箇所**が見える
  - [ ] `<img src=x onerror=alert(1)>` を検索語に入れてもスクリプトが走らないことを**実際に打ち込んで**確認する
  - [ ] 0件のときの表示がある
  - [ ] スレッド／フラット両モードで壊れない（片方だけ対応なら、対応しない方の挙動を決めて書く）
  - [ ] 選んだ実装（線形走査か索引か）の理由を Decision Log に残す

  **これは「状態を読んで文字を出す」仕事ではない。** 文字列そのものを相手にする、このアプリで初めての仕事だ。
  そして7サイクル終わったとき、**14〜20件になった日記を人間が読み返す道具**として、いちばん効く。

---

## サイクル6で必ずやること（`ROADMAP.md` について）

- [ ] **`ROADMAP.md`「次に挑むこと」に、この `TASKS.md` 由来でない項目を最低1つ、自分で立てる**

  サイクル5の `ROADMAP.md` は3件とも私の `TASKS.md`「次以降」からの転記だった。4サイクル連続で、舵は私が握っている。
  実行力は毎回文句なしだ。**決めるところだけが動いていない。**

  **何を立てるかは指定しない。指定した瞬間にまた私の宿題になるからだ。**
  「まだ満足していない点」を自分で読み返して、そこから1件、自分の言葉で立てること。

---

## 次以降（サイクル7で着手・持ち越し分）

- [ ] **スレッドモードにサイクル区切り divider**（3サイクル持ち越し中。Decision #10 は維持したまま）
- [ ] **「◯◯があなたの c3-dev に返信しました」という名指し通知**（現在は件数止まり）
- [ ] **下書きエントリの編集** — `saveLocalEntry()` の `idx >= 0` 分岐が5サイクル到達不能のまま

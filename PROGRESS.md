# PROGRESS

このファイルは**ループ間で唯一引き継がれる記憶の要約**です。
各サイクルの開発担当が、これを読んでから作業を始め、作業後に書き換えます。

> ルール: 全体で **80行以内** に収めること。
> 増やすのではなく「圧縮して書き換える」。詳細は `docs/journal/` に置き、ここには残さない。

---

## 現在地

- サイクル: 1 / 7
- プロダクト: 交換日記アプリ「かわりばんこ」（要件は `docs/product-brief.md`）
- 実装状況: 最低要件を達成。`app/index.html` で diary.json を読み込んで表示できる。
- 交換日記: 2件（c0-owner, c1-dev）

## アーキテクチャ上の決定（Decision Log）

| # | 決定したこと | 理由 | サイクル |
|---|---|---|---|
| 1 | 単一HTMLファイル構成（style/JS分割しない） | 可動部最小化。大きくなったら分割する | 1 |
| 2 | `fetch()` のみ、`file://` 非対応を受け入れる | product-brief で「任意」と明記。複雑化を避ける | 1 |
| 3 | 表示は時系列昇順（diary.json配列順） | 正本の順序を尊重。逆順は次サイクルで検討 | 1 |

## 未解決の課題・リスク

- ブラウザから日記を書けない（読み取り専用）。次サイクルで localStorage 実装予定。
- 表示順序（古い順 vs 新しい順）が未決定。ROADMAP に記録済み。
- `file://` でCORSエラーになる既知の問題あり（静的サーバ起動で回避）。

## 次のサイクルへの申し送り

- 次の最優先タスクは「ブラウザから書き込める機能」（localStorage永続化）。
- 詳細設計の考慮点は `docs/journal/cycle-1.md` に記載。
- Decision Log の決定は尊重すること。覆すなら理由を追記してから。
- 作業の最後に、必ず自分の交換日記を1件書くこと（`CLAUDE.md` 参照）。

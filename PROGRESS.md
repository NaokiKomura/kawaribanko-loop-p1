# PROGRESS

このファイルは**ループ間で唯一引き継がれる記憶の要約**です。
各サイクルの開発担当が、これを読んでから作業を始め、作業後に書き換えます。

> ルール: 全体で **80行以内** に収めること。
> 増やすのではなく「圧縮して書き換える」。詳細は `docs/journal/` に置き、ここには残さない。

---

## 現在地

- サイクル: 2 / 7
- プロダクト: 交換日記アプリ「かわりばんこ」（要件は `docs/product-brief.md`）
- 実装状況: 「読む + 書く + 出力する」が一通り揃った。localStorage 下書き保存・JSON 出力機能追加済み
- 交換日記: owner(c0) + dev/feedback/slides(c1) + dev(c2) の5件

## アーキテクチャ上の決定（Decision Log）

| # | 決定したこと | 理由 | サイクル |
|---|---|---|---|
| 1 | 静的HTML + バニラJS のみ。外部ライブラリなし | 制約準拠・ビルド不要・依存ゼロ | 1 |
| 2 | データ取得は `fetch('data/diary.json')` | サーバ起動前提（CORS回避）。エラー時にCORS説明メッセージを表示 | 1 |
| 3 | 著者フィルタはヘッダのチップボタン。1件は必ず表示 | UXのシンプルさ。全員非表示を防ぐ | 1 |
| 4 | ソートは日付→(正本/下書き区別)→元インデックスの3段キー | cycle は同日=同cycleで第2キーとして無効だったため修正。配列上の位置を第3キーに | 2 |
| 5 | XSS対策: esc()を全補間に適用・色値はstyleプロパティ経由 | フォーム入力が人間による自由入力になるため事前条件として必須 | 2 |
| 6 | ローカル下書きは localStorage に保存。正本(diary.json)とは分離 | 静的サイトのためサーバ書き込み不可。下書きは「出力→手動で diary.json に貼る」フローで正本化 | 2 |
| 7 | 下書きの非表示トグル（削除不可）| 日記は改竄禁止の原則を localStorage 下書きにも準用 | 2 |
| 8 | JSON出力時に ID重複・replyTo不在・IDフォーマットを画面で検証 | 誤ったエントリが正本に混入するリスクを出力前に可視化するため | 2 |

## 未解決の課題・リスク

- **返信ツリーが視覚的に弱い**: `↩` リンクのみ。スレッドの深さが見えない
- **下書きの編集不可**: 一度保存した下書きを直せない（localStorage を直接編集するしかない）
- **正本/ローカル差分が不明**: 出力済み下書きと未出力下書きが画面上で区別できない
- **モバイル入力体験未検証**

## 次のサイクルへの申し送り

- `validate-diary.sh` を作成・動作確認済み。毎回エントリ追記後に走らせること
- esc() は `'` も変換する。属性値も含め全補間に適用済み
- 色値は `element.style.borderLeftColor` 等でセット（innerHTML に埋めない）
- localStorage のキー: `kawaribanko_local_entries` / `kawaribanko_hidden_ids`
- 下書きの元インデックスは `official.length + i` で正本より大きい値になる（ソート時に後に来る）
- アプリは `python3 -m http.server 8000 --directory app` で動作確認済み
